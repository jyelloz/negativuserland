/*
 * Generated by gdbus-codegen 2.40.0. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "nul-media-player.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.mpris.MediaPlayer2.Player
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:NulNulMediaPlayer
 * @title: NulNulMediaPlayer
 * @short_description: Generated C code for the org.mpris.MediaPlayer2.Player D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Player.top_of_page">org.mpris.MediaPlayer2.Player</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.mpris.MediaPlayer2.Player ---- */

static const _ExtendedGDBusMethodInfo _nul_nul_media_player_method_info_next =
{
  {
    -1,
    (gchar *) "Next",
    NULL,
    NULL,
    NULL
  },
  "handle-next",
  FALSE
};

static const _ExtendedGDBusMethodInfo _nul_nul_media_player_method_info_previous =
{
  {
    -1,
    (gchar *) "Previous",
    NULL,
    NULL,
    NULL
  },
  "handle-previous",
  FALSE
};

static const _ExtendedGDBusMethodInfo _nul_nul_media_player_method_info_pause =
{
  {
    -1,
    (gchar *) "Pause",
    NULL,
    NULL,
    NULL
  },
  "handle-pause",
  FALSE
};

static const _ExtendedGDBusMethodInfo _nul_nul_media_player_method_info_play_pause =
{
  {
    -1,
    (gchar *) "PlayPause",
    NULL,
    NULL,
    NULL
  },
  "handle-play-pause",
  FALSE
};

static const _ExtendedGDBusMethodInfo _nul_nul_media_player_method_info_stop =
{
  {
    -1,
    (gchar *) "Stop",
    NULL,
    NULL,
    NULL
  },
  "handle-stop",
  FALSE
};

static const _ExtendedGDBusMethodInfo _nul_nul_media_player_method_info_play =
{
  {
    -1,
    (gchar *) "Play",
    NULL,
    NULL,
    NULL
  },
  "handle-play",
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_nul_media_player_method_info_seek_IN_ARG_Offset =
{
  {
    -1,
    (gchar *) "Offset",
    (gchar *) "x",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_nul_media_player_method_info_seek_IN_ARG_pointers[] =
{
  &_nul_nul_media_player_method_info_seek_IN_ARG_Offset,
  NULL
};

static const _ExtendedGDBusMethodInfo _nul_nul_media_player_method_info_seek =
{
  {
    -1,
    (gchar *) "Seek",
    (GDBusArgInfo **) &_nul_nul_media_player_method_info_seek_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-seek",
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_nul_media_player_method_info_set_position_IN_ARG_TrackId =
{
  {
    -1,
    (gchar *) "TrackId",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_nul_media_player_method_info_set_position_IN_ARG_Position =
{
  {
    -1,
    (gchar *) "Position",
    (gchar *) "x",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_nul_media_player_method_info_set_position_IN_ARG_pointers[] =
{
  &_nul_nul_media_player_method_info_set_position_IN_ARG_TrackId,
  &_nul_nul_media_player_method_info_set_position_IN_ARG_Position,
  NULL
};

static const _ExtendedGDBusMethodInfo _nul_nul_media_player_method_info_set_position =
{
  {
    -1,
    (gchar *) "SetPosition",
    (GDBusArgInfo **) &_nul_nul_media_player_method_info_set_position_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-position",
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_nul_media_player_method_info_open_uri_IN_ARG_Uri =
{
  {
    -1,
    (gchar *) "Uri",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_nul_media_player_method_info_open_uri_IN_ARG_pointers[] =
{
  &_nul_nul_media_player_method_info_open_uri_IN_ARG_Uri,
  NULL
};

static const _ExtendedGDBusMethodInfo _nul_nul_media_player_method_info_open_uri =
{
  {
    -1,
    (gchar *) "OpenUri",
    (GDBusArgInfo **) &_nul_nul_media_player_method_info_open_uri_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-open-uri",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _nul_nul_media_player_method_info_pointers[] =
{
  &_nul_nul_media_player_method_info_next,
  &_nul_nul_media_player_method_info_previous,
  &_nul_nul_media_player_method_info_pause,
  &_nul_nul_media_player_method_info_play_pause,
  &_nul_nul_media_player_method_info_stop,
  &_nul_nul_media_player_method_info_play,
  &_nul_nul_media_player_method_info_seek,
  &_nul_nul_media_player_method_info_set_position,
  &_nul_nul_media_player_method_info_open_uri,
  NULL
};

static const _ExtendedGDBusArgInfo _nul_nul_media_player_signal_info_seeked_ARG_Position =
{
  {
    -1,
    (gchar *) "Position",
    (gchar *) "x",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_nul_media_player_signal_info_seeked_ARG_pointers[] =
{
  &_nul_nul_media_player_signal_info_seeked_ARG_Position,
  NULL
};

static const _ExtendedGDBusSignalInfo _nul_nul_media_player_signal_info_seeked =
{
  {
    -1,
    (gchar *) "Seeked",
    (GDBusArgInfo **) &_nul_nul_media_player_signal_info_seeked_ARG_pointers,
    NULL
  },
  "seeked"
};

static const _ExtendedGDBusSignalInfo * const _nul_nul_media_player_signal_info_pointers[] =
{
  &_nul_nul_media_player_signal_info_seeked,
  NULL
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_playback_status_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_nul_media_player_property_playback_status_annotation_info_pointers[] =
{
  &_nul_nul_media_player_property_playback_status_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_nul_media_player_property_info_playback_status =
{
  {
    -1,
    (gchar *) "PlaybackStatus",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_nul_media_player_property_playback_status_annotation_info_pointers
  },
  "playback-status",
  FALSE
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_loop_status_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_loop_status_annotation_info_1 =
{
  -1,
  (gchar *) "org.mpris.MediaPlayer2.property.optional",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_nul_media_player_property_loop_status_annotation_info_pointers[] =
{
  &_nul_nul_media_player_property_loop_status_annotation_info_0,
  &_nul_nul_media_player_property_loop_status_annotation_info_1,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_nul_media_player_property_info_loop_status =
{
  {
    -1,
    (gchar *) "LoopStatus",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    (GDBusAnnotationInfo **) &_nul_nul_media_player_property_loop_status_annotation_info_pointers
  },
  "loop-status",
  FALSE
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_rate_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_nul_media_player_property_rate_annotation_info_pointers[] =
{
  &_nul_nul_media_player_property_rate_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_nul_media_player_property_info_rate =
{
  {
    -1,
    (gchar *) "Rate",
    (gchar *) "d",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    (GDBusAnnotationInfo **) &_nul_nul_media_player_property_rate_annotation_info_pointers
  },
  "rate",
  FALSE
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_shuffle_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_shuffle_annotation_info_1 =
{
  -1,
  (gchar *) "org.mpris.MediaPlayer2.property.optional",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_nul_media_player_property_shuffle_annotation_info_pointers[] =
{
  &_nul_nul_media_player_property_shuffle_annotation_info_0,
  &_nul_nul_media_player_property_shuffle_annotation_info_1,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_nul_media_player_property_info_shuffle =
{
  {
    -1,
    (gchar *) "Shuffle",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    (GDBusAnnotationInfo **) &_nul_nul_media_player_property_shuffle_annotation_info_pointers
  },
  "shuffle",
  FALSE
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_metadata_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_nul_media_player_property_metadata_annotation_info_pointers[] =
{
  &_nul_nul_media_player_property_metadata_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_nul_media_player_property_info_metadata =
{
  {
    -1,
    (gchar *) "Metadata",
    (gchar *) "a{sv}",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_nul_media_player_property_metadata_annotation_info_pointers
  },
  "metadata",
  FALSE
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_volume_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_nul_media_player_property_volume_annotation_info_pointers[] =
{
  &_nul_nul_media_player_property_volume_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_nul_media_player_property_info_volume =
{
  {
    -1,
    (gchar *) "Volume",
    (gchar *) "d",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    (GDBusAnnotationInfo **) &_nul_nul_media_player_property_volume_annotation_info_pointers
  },
  "volume",
  FALSE
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_position_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "false",
  NULL
};

static const GDBusAnnotationInfo * const _nul_nul_media_player_property_position_annotation_info_pointers[] =
{
  &_nul_nul_media_player_property_position_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_nul_media_player_property_info_position =
{
  {
    -1,
    (gchar *) "Position",
    (gchar *) "x",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_nul_media_player_property_position_annotation_info_pointers
  },
  "position",
  FALSE
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_minimum_rate_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_nul_media_player_property_minimum_rate_annotation_info_pointers[] =
{
  &_nul_nul_media_player_property_minimum_rate_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_nul_media_player_property_info_minimum_rate =
{
  {
    -1,
    (gchar *) "MinimumRate",
    (gchar *) "d",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_nul_media_player_property_minimum_rate_annotation_info_pointers
  },
  "minimum-rate",
  FALSE
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_maximum_rate_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_nul_media_player_property_maximum_rate_annotation_info_pointers[] =
{
  &_nul_nul_media_player_property_maximum_rate_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_nul_media_player_property_info_maximum_rate =
{
  {
    -1,
    (gchar *) "MaximumRate",
    (gchar *) "d",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_nul_media_player_property_maximum_rate_annotation_info_pointers
  },
  "maximum-rate",
  FALSE
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_can_go_next_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_nul_media_player_property_can_go_next_annotation_info_pointers[] =
{
  &_nul_nul_media_player_property_can_go_next_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_nul_media_player_property_info_can_go_next =
{
  {
    -1,
    (gchar *) "CanGoNext",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_nul_media_player_property_can_go_next_annotation_info_pointers
  },
  "can-go-next",
  FALSE
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_can_go_previous_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_nul_media_player_property_can_go_previous_annotation_info_pointers[] =
{
  &_nul_nul_media_player_property_can_go_previous_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_nul_media_player_property_info_can_go_previous =
{
  {
    -1,
    (gchar *) "CanGoPrevious",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_nul_media_player_property_can_go_previous_annotation_info_pointers
  },
  "can-go-previous",
  FALSE
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_can_play_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_nul_media_player_property_can_play_annotation_info_pointers[] =
{
  &_nul_nul_media_player_property_can_play_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_nul_media_player_property_info_can_play =
{
  {
    -1,
    (gchar *) "CanPlay",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_nul_media_player_property_can_play_annotation_info_pointers
  },
  "can-play",
  FALSE
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_can_pause_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_nul_media_player_property_can_pause_annotation_info_pointers[] =
{
  &_nul_nul_media_player_property_can_pause_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_nul_media_player_property_info_can_pause =
{
  {
    -1,
    (gchar *) "CanPause",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_nul_media_player_property_can_pause_annotation_info_pointers
  },
  "can-pause",
  FALSE
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_can_seek_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_nul_media_player_property_can_seek_annotation_info_pointers[] =
{
  &_nul_nul_media_player_property_can_seek_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_nul_media_player_property_info_can_seek =
{
  {
    -1,
    (gchar *) "CanSeek",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_nul_media_player_property_can_seek_annotation_info_pointers
  },
  "can-seek",
  FALSE
};

static const GDBusAnnotationInfo _nul_nul_media_player_property_can_control_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "false",
  NULL
};

static const GDBusAnnotationInfo * const _nul_nul_media_player_property_can_control_annotation_info_pointers[] =
{
  &_nul_nul_media_player_property_can_control_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_nul_media_player_property_info_can_control =
{
  {
    -1,
    (gchar *) "CanControl",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_nul_media_player_property_can_control_annotation_info_pointers
  },
  "can-control",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _nul_nul_media_player_property_info_pointers[] =
{
  &_nul_nul_media_player_property_info_playback_status,
  &_nul_nul_media_player_property_info_loop_status,
  &_nul_nul_media_player_property_info_rate,
  &_nul_nul_media_player_property_info_shuffle,
  &_nul_nul_media_player_property_info_metadata,
  &_nul_nul_media_player_property_info_volume,
  &_nul_nul_media_player_property_info_position,
  &_nul_nul_media_player_property_info_minimum_rate,
  &_nul_nul_media_player_property_info_maximum_rate,
  &_nul_nul_media_player_property_info_can_go_next,
  &_nul_nul_media_player_property_info_can_go_previous,
  &_nul_nul_media_player_property_info_can_play,
  &_nul_nul_media_player_property_info_can_pause,
  &_nul_nul_media_player_property_info_can_seek,
  &_nul_nul_media_player_property_info_can_control,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _nul_nul_media_player_interface_info =
{
  {
    -1,
    (gchar *) "org.mpris.MediaPlayer2.Player",
    (GDBusMethodInfo **) &_nul_nul_media_player_method_info_pointers,
    (GDBusSignalInfo **) &_nul_nul_media_player_signal_info_pointers,
    (GDBusPropertyInfo **) &_nul_nul_media_player_property_info_pointers,
    NULL
  },
  "nul-media-player",
};


/**
 * nul_nul_media_player_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Player.top_of_page">org.mpris.MediaPlayer2.Player</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
nul_nul_media_player_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_nul_nul_media_player_interface_info.parent_struct;
}

/**
 * nul_nul_media_player_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #NulNulMediaPlayer interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
nul_nul_media_player_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "playback-status");
  g_object_class_override_property (klass, property_id_begin++, "loop-status");
  g_object_class_override_property (klass, property_id_begin++, "rate");
  g_object_class_override_property (klass, property_id_begin++, "shuffle");
  g_object_class_override_property (klass, property_id_begin++, "metadata");
  g_object_class_override_property (klass, property_id_begin++, "volume");
  g_object_class_override_property (klass, property_id_begin++, "position");
  g_object_class_override_property (klass, property_id_begin++, "minimum-rate");
  g_object_class_override_property (klass, property_id_begin++, "maximum-rate");
  g_object_class_override_property (klass, property_id_begin++, "can-go-next");
  g_object_class_override_property (klass, property_id_begin++, "can-go-previous");
  g_object_class_override_property (klass, property_id_begin++, "can-play");
  g_object_class_override_property (klass, property_id_begin++, "can-pause");
  g_object_class_override_property (klass, property_id_begin++, "can-seek");
  g_object_class_override_property (klass, property_id_begin++, "can-control");
  return property_id_begin - 1;
}



/**
 * NulNulMediaPlayer:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Player.top_of_page">org.mpris.MediaPlayer2.Player</link>.
 */

/**
 * NulNulMediaPlayerIface:
 * @parent_iface: The parent interface.
 * @handle_next: Handler for the #NulNulMediaPlayer::handle-next signal.
 * @handle_open_uri: Handler for the #NulNulMediaPlayer::handle-open-uri signal.
 * @handle_pause: Handler for the #NulNulMediaPlayer::handle-pause signal.
 * @handle_play: Handler for the #NulNulMediaPlayer::handle-play signal.
 * @handle_play_pause: Handler for the #NulNulMediaPlayer::handle-play-pause signal.
 * @handle_previous: Handler for the #NulNulMediaPlayer::handle-previous signal.
 * @handle_seek: Handler for the #NulNulMediaPlayer::handle-seek signal.
 * @handle_set_position: Handler for the #NulNulMediaPlayer::handle-set-position signal.
 * @handle_stop: Handler for the #NulNulMediaPlayer::handle-stop signal.
 * @get_can_control: Getter for the #NulNulMediaPlayer:can-control property.
 * @get_can_go_next: Getter for the #NulNulMediaPlayer:can-go-next property.
 * @get_can_go_previous: Getter for the #NulNulMediaPlayer:can-go-previous property.
 * @get_can_pause: Getter for the #NulNulMediaPlayer:can-pause property.
 * @get_can_play: Getter for the #NulNulMediaPlayer:can-play property.
 * @get_can_seek: Getter for the #NulNulMediaPlayer:can-seek property.
 * @get_loop_status: Getter for the #NulNulMediaPlayer:loop-status property.
 * @get_maximum_rate: Getter for the #NulNulMediaPlayer:maximum-rate property.
 * @get_metadata: Getter for the #NulNulMediaPlayer:metadata property.
 * @get_minimum_rate: Getter for the #NulNulMediaPlayer:minimum-rate property.
 * @get_playback_status: Getter for the #NulNulMediaPlayer:playback-status property.
 * @get_position: Getter for the #NulNulMediaPlayer:position property.
 * @get_rate: Getter for the #NulNulMediaPlayer:rate property.
 * @get_shuffle: Getter for the #NulNulMediaPlayer:shuffle property.
 * @get_volume: Getter for the #NulNulMediaPlayer:volume property.
 * @seeked: Handler for the #NulNulMediaPlayer::seeked signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Player.top_of_page">org.mpris.MediaPlayer2.Player</link>.
 */

typedef NulNulMediaPlayerIface NulNulMediaPlayerInterface;
G_DEFINE_INTERFACE (NulNulMediaPlayer, nul_nul_media_player, G_TYPE_OBJECT);

static void
nul_nul_media_player_default_init (NulNulMediaPlayerIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * NulNulMediaPlayer::handle-next:
   * @object: A #NulNulMediaPlayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Next">Next()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_nul_media_player_complete_next() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-next",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulNulMediaPlayerIface, handle_next),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * NulNulMediaPlayer::handle-previous:
   * @object: A #NulNulMediaPlayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Previous">Previous()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_nul_media_player_complete_previous() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-previous",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulNulMediaPlayerIface, handle_previous),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * NulNulMediaPlayer::handle-pause:
   * @object: A #NulNulMediaPlayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Pause">Pause()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_nul_media_player_complete_pause() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-pause",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulNulMediaPlayerIface, handle_pause),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * NulNulMediaPlayer::handle-play-pause:
   * @object: A #NulNulMediaPlayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.PlayPause">PlayPause()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_nul_media_player_complete_play_pause() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-play-pause",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulNulMediaPlayerIface, handle_play_pause),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * NulNulMediaPlayer::handle-stop:
   * @object: A #NulNulMediaPlayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Stop">Stop()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_nul_media_player_complete_stop() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-stop",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulNulMediaPlayerIface, handle_stop),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * NulNulMediaPlayer::handle-play:
   * @object: A #NulNulMediaPlayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Play">Play()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_nul_media_player_complete_play() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-play",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulNulMediaPlayerIface, handle_play),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * NulNulMediaPlayer::handle-seek:
   * @object: A #NulNulMediaPlayer.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_Offset: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Seek">Seek()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_nul_media_player_complete_seek() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-seek",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulNulMediaPlayerIface, handle_seek),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_INT64);

  /**
   * NulNulMediaPlayer::handle-set-position:
   * @object: A #NulNulMediaPlayer.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_TrackId: Argument passed by remote caller.
   * @arg_Position: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.SetPosition">SetPosition()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_nul_media_player_complete_set_position() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-position",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulNulMediaPlayerIface, handle_set_position),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_INT64);

  /**
   * NulNulMediaPlayer::handle-open-uri:
   * @object: A #NulNulMediaPlayer.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_Uri: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.OpenUri">OpenUri()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_nul_media_player_complete_open_uri() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-open-uri",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulNulMediaPlayerIface, handle_open_uri),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /* GObject signals for received D-Bus signals: */
  /**
   * NulNulMediaPlayer::seeked:
   * @object: A #NulNulMediaPlayer.
   * @arg_Position: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-mpris-MediaPlayer2-Player.Seeked">"Seeked"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("seeked",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulNulMediaPlayerIface, seeked),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_INT64);

  /* GObject properties for D-Bus properties: */
  /**
   * NulNulMediaPlayer:playback-status:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.PlaybackStatus">"PlaybackStatus"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("playback-status", "PlaybackStatus", "PlaybackStatus", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulNulMediaPlayer:loop-status:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.LoopStatus">"LoopStatus"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("loop-status", "LoopStatus", "LoopStatus", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulNulMediaPlayer:rate:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Rate">"Rate"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_double ("rate", "Rate", "Rate", -G_MAXDOUBLE, G_MAXDOUBLE, 0.0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulNulMediaPlayer:shuffle:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Shuffle">"Shuffle"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("shuffle", "Shuffle", "Shuffle", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulNulMediaPlayer:metadata:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Metadata">"Metadata"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_variant ("metadata", "Metadata", "Metadata", G_VARIANT_TYPE ("a{sv}"), NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulNulMediaPlayer:volume:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Volume">"Volume"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_double ("volume", "Volume", "Volume", -G_MAXDOUBLE, G_MAXDOUBLE, 0.0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulNulMediaPlayer:position:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Position">"Position"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int64 ("position", "Position", "Position", G_MININT64, G_MAXINT64, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulNulMediaPlayer:minimum-rate:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.MinimumRate">"MinimumRate"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_double ("minimum-rate", "MinimumRate", "MinimumRate", -G_MAXDOUBLE, G_MAXDOUBLE, 0.0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulNulMediaPlayer:maximum-rate:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.MaximumRate">"MaximumRate"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_double ("maximum-rate", "MaximumRate", "MaximumRate", -G_MAXDOUBLE, G_MAXDOUBLE, 0.0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulNulMediaPlayer:can-go-next:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanGoNext">"CanGoNext"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-go-next", "CanGoNext", "CanGoNext", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulNulMediaPlayer:can-go-previous:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanGoPrevious">"CanGoPrevious"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-go-previous", "CanGoPrevious", "CanGoPrevious", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulNulMediaPlayer:can-play:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanPlay">"CanPlay"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-play", "CanPlay", "CanPlay", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulNulMediaPlayer:can-pause:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanPause">"CanPause"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-pause", "CanPause", "CanPause", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulNulMediaPlayer:can-seek:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanSeek">"CanSeek"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-seek", "CanSeek", "CanSeek", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulNulMediaPlayer:can-control:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanControl">"CanControl"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-control", "CanControl", "CanControl", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * nul_nul_media_player_get_playback_status: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.PlaybackStatus">"PlaybackStatus"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nul_nul_media_player_dup_playback_status() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nul_nul_media_player_get_playback_status (NulNulMediaPlayer *object)
{
  return NUL_NUL_MEDIA_PLAYER_GET_IFACE (object)->get_playback_status (object);
}

/**
 * nul_nul_media_player_dup_playback_status: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.PlaybackStatus">"PlaybackStatus"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nul_nul_media_player_dup_playback_status (NulNulMediaPlayer *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "playback-status", &value, NULL);
  return value;
}

/**
 * nul_nul_media_player_set_playback_status: (skip)
 * @object: A #NulNulMediaPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.PlaybackStatus">"PlaybackStatus"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_nul_media_player_set_playback_status (NulNulMediaPlayer *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "playback-status", value, NULL);
}

/**
 * nul_nul_media_player_get_loop_status: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.LoopStatus">"LoopStatus"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nul_nul_media_player_dup_loop_status() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nul_nul_media_player_get_loop_status (NulNulMediaPlayer *object)
{
  return NUL_NUL_MEDIA_PLAYER_GET_IFACE (object)->get_loop_status (object);
}

/**
 * nul_nul_media_player_dup_loop_status: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.LoopStatus">"LoopStatus"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nul_nul_media_player_dup_loop_status (NulNulMediaPlayer *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "loop-status", &value, NULL);
  return value;
}

/**
 * nul_nul_media_player_set_loop_status: (skip)
 * @object: A #NulNulMediaPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.LoopStatus">"LoopStatus"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
nul_nul_media_player_set_loop_status (NulNulMediaPlayer *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "loop-status", value, NULL);
}

/**
 * nul_nul_media_player_get_rate: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Rate">"Rate"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gdouble
nul_nul_media_player_get_rate (NulNulMediaPlayer *object)
{
  return NUL_NUL_MEDIA_PLAYER_GET_IFACE (object)->get_rate (object);
}

/**
 * nul_nul_media_player_set_rate: (skip)
 * @object: A #NulNulMediaPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Rate">"Rate"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
nul_nul_media_player_set_rate (NulNulMediaPlayer *object, gdouble value)
{
  g_object_set (G_OBJECT (object), "rate", value, NULL);
}

/**
 * nul_nul_media_player_get_shuffle: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Shuffle">"Shuffle"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean
nul_nul_media_player_get_shuffle (NulNulMediaPlayer *object)
{
  return NUL_NUL_MEDIA_PLAYER_GET_IFACE (object)->get_shuffle (object);
}

/**
 * nul_nul_media_player_set_shuffle: (skip)
 * @object: A #NulNulMediaPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Shuffle">"Shuffle"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
nul_nul_media_player_set_shuffle (NulNulMediaPlayer *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "shuffle", value, NULL);
}

/**
 * nul_nul_media_player_get_metadata: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Metadata">"Metadata"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nul_nul_media_player_dup_metadata() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
GVariant *
nul_nul_media_player_get_metadata (NulNulMediaPlayer *object)
{
  return NUL_NUL_MEDIA_PLAYER_GET_IFACE (object)->get_metadata (object);
}

/**
 * nul_nul_media_player_dup_metadata: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Metadata">"Metadata"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_variant_unref().
 */
GVariant *
nul_nul_media_player_dup_metadata (NulNulMediaPlayer *object)
{
  GVariant *value;
  g_object_get (G_OBJECT (object), "metadata", &value, NULL);
  return value;
}

/**
 * nul_nul_media_player_set_metadata: (skip)
 * @object: A #NulNulMediaPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Metadata">"Metadata"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_nul_media_player_set_metadata (NulNulMediaPlayer *object, GVariant *value)
{
  g_object_set (G_OBJECT (object), "metadata", value, NULL);
}

/**
 * nul_nul_media_player_get_volume: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Volume">"Volume"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gdouble
nul_nul_media_player_get_volume (NulNulMediaPlayer *object)
{
  return NUL_NUL_MEDIA_PLAYER_GET_IFACE (object)->get_volume (object);
}

/**
 * nul_nul_media_player_set_volume: (skip)
 * @object: A #NulNulMediaPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Volume">"Volume"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
nul_nul_media_player_set_volume (NulNulMediaPlayer *object, gdouble value)
{
  g_object_set (G_OBJECT (object), "volume", value, NULL);
}

/**
 * nul_nul_media_player_get_position: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Position">"Position"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint64
nul_nul_media_player_get_position (NulNulMediaPlayer *object)
{
  return NUL_NUL_MEDIA_PLAYER_GET_IFACE (object)->get_position (object);
}

/**
 * nul_nul_media_player_set_position: (skip)
 * @object: A #NulNulMediaPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Position">"Position"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_nul_media_player_set_position (NulNulMediaPlayer *object, gint64 value)
{
  g_object_set (G_OBJECT (object), "position", value, NULL);
}

/**
 * nul_nul_media_player_get_minimum_rate: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.MinimumRate">"MinimumRate"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gdouble
nul_nul_media_player_get_minimum_rate (NulNulMediaPlayer *object)
{
  return NUL_NUL_MEDIA_PLAYER_GET_IFACE (object)->get_minimum_rate (object);
}

/**
 * nul_nul_media_player_set_minimum_rate: (skip)
 * @object: A #NulNulMediaPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.MinimumRate">"MinimumRate"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_nul_media_player_set_minimum_rate (NulNulMediaPlayer *object, gdouble value)
{
  g_object_set (G_OBJECT (object), "minimum-rate", value, NULL);
}

/**
 * nul_nul_media_player_get_maximum_rate: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.MaximumRate">"MaximumRate"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gdouble
nul_nul_media_player_get_maximum_rate (NulNulMediaPlayer *object)
{
  return NUL_NUL_MEDIA_PLAYER_GET_IFACE (object)->get_maximum_rate (object);
}

/**
 * nul_nul_media_player_set_maximum_rate: (skip)
 * @object: A #NulNulMediaPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.MaximumRate">"MaximumRate"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_nul_media_player_set_maximum_rate (NulNulMediaPlayer *object, gdouble value)
{
  g_object_set (G_OBJECT (object), "maximum-rate", value, NULL);
}

/**
 * nul_nul_media_player_get_can_go_next: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanGoNext">"CanGoNext"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean
nul_nul_media_player_get_can_go_next (NulNulMediaPlayer *object)
{
  return NUL_NUL_MEDIA_PLAYER_GET_IFACE (object)->get_can_go_next (object);
}

/**
 * nul_nul_media_player_set_can_go_next: (skip)
 * @object: A #NulNulMediaPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanGoNext">"CanGoNext"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_nul_media_player_set_can_go_next (NulNulMediaPlayer *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-go-next", value, NULL);
}

/**
 * nul_nul_media_player_get_can_go_previous: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanGoPrevious">"CanGoPrevious"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean
nul_nul_media_player_get_can_go_previous (NulNulMediaPlayer *object)
{
  return NUL_NUL_MEDIA_PLAYER_GET_IFACE (object)->get_can_go_previous (object);
}

/**
 * nul_nul_media_player_set_can_go_previous: (skip)
 * @object: A #NulNulMediaPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanGoPrevious">"CanGoPrevious"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_nul_media_player_set_can_go_previous (NulNulMediaPlayer *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-go-previous", value, NULL);
}

/**
 * nul_nul_media_player_get_can_play: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanPlay">"CanPlay"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean
nul_nul_media_player_get_can_play (NulNulMediaPlayer *object)
{
  return NUL_NUL_MEDIA_PLAYER_GET_IFACE (object)->get_can_play (object);
}

/**
 * nul_nul_media_player_set_can_play: (skip)
 * @object: A #NulNulMediaPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanPlay">"CanPlay"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_nul_media_player_set_can_play (NulNulMediaPlayer *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-play", value, NULL);
}

/**
 * nul_nul_media_player_get_can_pause: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanPause">"CanPause"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean
nul_nul_media_player_get_can_pause (NulNulMediaPlayer *object)
{
  return NUL_NUL_MEDIA_PLAYER_GET_IFACE (object)->get_can_pause (object);
}

/**
 * nul_nul_media_player_set_can_pause: (skip)
 * @object: A #NulNulMediaPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanPause">"CanPause"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_nul_media_player_set_can_pause (NulNulMediaPlayer *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-pause", value, NULL);
}

/**
 * nul_nul_media_player_get_can_seek: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanSeek">"CanSeek"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean
nul_nul_media_player_get_can_seek (NulNulMediaPlayer *object)
{
  return NUL_NUL_MEDIA_PLAYER_GET_IFACE (object)->get_can_seek (object);
}

/**
 * nul_nul_media_player_set_can_seek: (skip)
 * @object: A #NulNulMediaPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanSeek">"CanSeek"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_nul_media_player_set_can_seek (NulNulMediaPlayer *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-seek", value, NULL);
}

/**
 * nul_nul_media_player_get_can_control: (skip)
 * @object: A #NulNulMediaPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanControl">"CanControl"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean
nul_nul_media_player_get_can_control (NulNulMediaPlayer *object)
{
  return NUL_NUL_MEDIA_PLAYER_GET_IFACE (object)->get_can_control (object);
}

/**
 * nul_nul_media_player_set_can_control: (skip)
 * @object: A #NulNulMediaPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanControl">"CanControl"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_nul_media_player_set_can_control (NulNulMediaPlayer *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-control", value, NULL);
}

/**
 * nul_nul_media_player_emit_seeked:
 * @object: A #NulNulMediaPlayer.
 * @arg_Position: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-mpris-MediaPlayer2-Player.Seeked">"Seeked"</link> D-Bus signal.
 */
void
nul_nul_media_player_emit_seeked (
    NulNulMediaPlayer *object,
    gint64 arg_Position)
{
  g_signal_emit_by_name (object, "seeked", arg_Position);
}

/**
 * nul_nul_media_player_call_next:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Next">Next()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_nul_media_player_call_next_finish() to get the result of the operation.
 *
 * See nul_nul_media_player_call_next_sync() for the synchronous, blocking version of this method.
 */
void
nul_nul_media_player_call_next (
    NulNulMediaPlayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Next",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_nul_media_player_call_next_finish:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_nul_media_player_call_next().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_nul_media_player_call_next().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_next_finish (
    NulNulMediaPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_next_sync:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Next">Next()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_nul_media_player_call_next() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_next_sync (
    NulNulMediaPlayer *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Next",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_previous:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Previous">Previous()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_nul_media_player_call_previous_finish() to get the result of the operation.
 *
 * See nul_nul_media_player_call_previous_sync() for the synchronous, blocking version of this method.
 */
void
nul_nul_media_player_call_previous (
    NulNulMediaPlayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Previous",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_nul_media_player_call_previous_finish:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_nul_media_player_call_previous().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_nul_media_player_call_previous().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_previous_finish (
    NulNulMediaPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_previous_sync:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Previous">Previous()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_nul_media_player_call_previous() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_previous_sync (
    NulNulMediaPlayer *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Previous",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_pause:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Pause">Pause()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_nul_media_player_call_pause_finish() to get the result of the operation.
 *
 * See nul_nul_media_player_call_pause_sync() for the synchronous, blocking version of this method.
 */
void
nul_nul_media_player_call_pause (
    NulNulMediaPlayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Pause",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_nul_media_player_call_pause_finish:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_nul_media_player_call_pause().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_nul_media_player_call_pause().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_pause_finish (
    NulNulMediaPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_pause_sync:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Pause">Pause()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_nul_media_player_call_pause() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_pause_sync (
    NulNulMediaPlayer *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Pause",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_play_pause:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.PlayPause">PlayPause()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_nul_media_player_call_play_pause_finish() to get the result of the operation.
 *
 * See nul_nul_media_player_call_play_pause_sync() for the synchronous, blocking version of this method.
 */
void
nul_nul_media_player_call_play_pause (
    NulNulMediaPlayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "PlayPause",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_nul_media_player_call_play_pause_finish:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_nul_media_player_call_play_pause().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_nul_media_player_call_play_pause().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_play_pause_finish (
    NulNulMediaPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_play_pause_sync:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.PlayPause">PlayPause()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_nul_media_player_call_play_pause() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_play_pause_sync (
    NulNulMediaPlayer *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "PlayPause",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_stop:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Stop">Stop()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_nul_media_player_call_stop_finish() to get the result of the operation.
 *
 * See nul_nul_media_player_call_stop_sync() for the synchronous, blocking version of this method.
 */
void
nul_nul_media_player_call_stop (
    NulNulMediaPlayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Stop",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_nul_media_player_call_stop_finish:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_nul_media_player_call_stop().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_nul_media_player_call_stop().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_stop_finish (
    NulNulMediaPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_stop_sync:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Stop">Stop()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_nul_media_player_call_stop() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_stop_sync (
    NulNulMediaPlayer *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Stop",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_play:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Play">Play()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_nul_media_player_call_play_finish() to get the result of the operation.
 *
 * See nul_nul_media_player_call_play_sync() for the synchronous, blocking version of this method.
 */
void
nul_nul_media_player_call_play (
    NulNulMediaPlayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Play",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_nul_media_player_call_play_finish:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_nul_media_player_call_play().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_nul_media_player_call_play().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_play_finish (
    NulNulMediaPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_play_sync:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Play">Play()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_nul_media_player_call_play() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_play_sync (
    NulNulMediaPlayer *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Play",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_seek:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @arg_Offset: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Seek">Seek()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_nul_media_player_call_seek_finish() to get the result of the operation.
 *
 * See nul_nul_media_player_call_seek_sync() for the synchronous, blocking version of this method.
 */
void
nul_nul_media_player_call_seek (
    NulNulMediaPlayer *proxy,
    gint64 arg_Offset,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Seek",
    g_variant_new ("(x)",
                   arg_Offset),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_nul_media_player_call_seek_finish:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_nul_media_player_call_seek().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_nul_media_player_call_seek().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_seek_finish (
    NulNulMediaPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_seek_sync:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @arg_Offset: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Seek">Seek()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_nul_media_player_call_seek() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_seek_sync (
    NulNulMediaPlayer *proxy,
    gint64 arg_Offset,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Seek",
    g_variant_new ("(x)",
                   arg_Offset),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_set_position:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @arg_TrackId: Argument to pass with the method invocation.
 * @arg_Position: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.SetPosition">SetPosition()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_nul_media_player_call_set_position_finish() to get the result of the operation.
 *
 * See nul_nul_media_player_call_set_position_sync() for the synchronous, blocking version of this method.
 */
void
nul_nul_media_player_call_set_position (
    NulNulMediaPlayer *proxy,
    const gchar *arg_TrackId,
    gint64 arg_Position,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetPosition",
    g_variant_new ("(ox)",
                   arg_TrackId,
                   arg_Position),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_nul_media_player_call_set_position_finish:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_nul_media_player_call_set_position().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_nul_media_player_call_set_position().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_set_position_finish (
    NulNulMediaPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_set_position_sync:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @arg_TrackId: Argument to pass with the method invocation.
 * @arg_Position: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.SetPosition">SetPosition()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_nul_media_player_call_set_position() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_set_position_sync (
    NulNulMediaPlayer *proxy,
    const gchar *arg_TrackId,
    gint64 arg_Position,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetPosition",
    g_variant_new ("(ox)",
                   arg_TrackId,
                   arg_Position),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_open_uri:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @arg_Uri: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.OpenUri">OpenUri()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_nul_media_player_call_open_uri_finish() to get the result of the operation.
 *
 * See nul_nul_media_player_call_open_uri_sync() for the synchronous, blocking version of this method.
 */
void
nul_nul_media_player_call_open_uri (
    NulNulMediaPlayer *proxy,
    const gchar *arg_Uri,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "OpenUri",
    g_variant_new ("(s)",
                   arg_Uri),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_nul_media_player_call_open_uri_finish:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_nul_media_player_call_open_uri().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_nul_media_player_call_open_uri().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_open_uri_finish (
    NulNulMediaPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_call_open_uri_sync:
 * @proxy: A #NulNulMediaPlayerProxy.
 * @arg_Uri: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.OpenUri">OpenUri()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_nul_media_player_call_open_uri() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_nul_media_player_call_open_uri_sync (
    NulNulMediaPlayer *proxy,
    const gchar *arg_Uri,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "OpenUri",
    g_variant_new ("(s)",
                   arg_Uri),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_nul_media_player_complete_next:
 * @object: A #NulNulMediaPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Next">Next()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_nul_media_player_complete_next (
    NulNulMediaPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * nul_nul_media_player_complete_previous:
 * @object: A #NulNulMediaPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Previous">Previous()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_nul_media_player_complete_previous (
    NulNulMediaPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * nul_nul_media_player_complete_pause:
 * @object: A #NulNulMediaPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Pause">Pause()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_nul_media_player_complete_pause (
    NulNulMediaPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * nul_nul_media_player_complete_play_pause:
 * @object: A #NulNulMediaPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.PlayPause">PlayPause()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_nul_media_player_complete_play_pause (
    NulNulMediaPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * nul_nul_media_player_complete_stop:
 * @object: A #NulNulMediaPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Stop">Stop()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_nul_media_player_complete_stop (
    NulNulMediaPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * nul_nul_media_player_complete_play:
 * @object: A #NulNulMediaPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Play">Play()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_nul_media_player_complete_play (
    NulNulMediaPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * nul_nul_media_player_complete_seek:
 * @object: A #NulNulMediaPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Seek">Seek()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_nul_media_player_complete_seek (
    NulNulMediaPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * nul_nul_media_player_complete_set_position:
 * @object: A #NulNulMediaPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.SetPosition">SetPosition()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_nul_media_player_complete_set_position (
    NulNulMediaPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * nul_nul_media_player_complete_open_uri:
 * @object: A #NulNulMediaPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.OpenUri">OpenUri()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_nul_media_player_complete_open_uri (
    NulNulMediaPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * NulNulMediaPlayerProxy:
 *
 * The #NulNulMediaPlayerProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * NulNulMediaPlayerProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #NulNulMediaPlayerProxy.
 */

struct _NulNulMediaPlayerProxyPrivate
{
  GData *qdata;
};

static void nul_nul_media_player_proxy_iface_init (NulNulMediaPlayerIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (NulNulMediaPlayerProxy, nul_nul_media_player_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (NulNulMediaPlayerProxy)
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_NUL_MEDIA_PLAYER, nul_nul_media_player_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (NulNulMediaPlayerProxy, nul_nul_media_player_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_NUL_MEDIA_PLAYER, nul_nul_media_player_proxy_iface_init));

#endif
static void
nul_nul_media_player_proxy_finalize (GObject *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (nul_nul_media_player_proxy_parent_class)->finalize (object);
}

static void
nul_nul_media_player_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 15);
  info = _nul_nul_media_player_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
nul_nul_media_player_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.mpris.MediaPlayer2.Player: %s (%s, %d)",
                 info->parent_struct.name,
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
nul_nul_media_player_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 15);
  info = _nul_nul_media_player_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.mpris.MediaPlayer2.Player", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) nul_nul_media_player_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
nul_nul_media_player_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_nul_nul_media_player_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], NUL_TYPE_NUL_MEDIA_PLAYER);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, NUL_TYPE_NUL_MEDIA_PLAYER);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
nul_nul_media_player_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_nul_media_player_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_nul_media_player_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
nul_nul_media_player_proxy_get_playback_status (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "PlaybackStatus");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
nul_nul_media_player_proxy_get_loop_status (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "LoopStatus");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gdouble
nul_nul_media_player_proxy_get_rate (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  GVariant *variant;
  gdouble value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Rate");
  if (variant != NULL)
    {
      value = g_variant_get_double (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean
nul_nul_media_player_proxy_get_shuffle (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Shuffle");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static GVariant *
nul_nul_media_player_proxy_get_metadata (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  GVariant *variant;
  GVariant *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Metadata");
  value = variant;
  if (variant != NULL)
    g_variant_unref (variant);
  return value;
}

static gdouble
nul_nul_media_player_proxy_get_volume (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  GVariant *variant;
  gdouble value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Volume");
  if (variant != NULL)
    {
      value = g_variant_get_double (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint64
nul_nul_media_player_proxy_get_position (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  GVariant *variant;
  gint64 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Position");
  if (variant != NULL)
    {
      value = g_variant_get_int64 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gdouble
nul_nul_media_player_proxy_get_minimum_rate (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  GVariant *variant;
  gdouble value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "MinimumRate");
  if (variant != NULL)
    {
      value = g_variant_get_double (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gdouble
nul_nul_media_player_proxy_get_maximum_rate (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  GVariant *variant;
  gdouble value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "MaximumRate");
  if (variant != NULL)
    {
      value = g_variant_get_double (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean
nul_nul_media_player_proxy_get_can_go_next (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanGoNext");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean
nul_nul_media_player_proxy_get_can_go_previous (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanGoPrevious");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean
nul_nul_media_player_proxy_get_can_play (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanPlay");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean
nul_nul_media_player_proxy_get_can_pause (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanPause");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean
nul_nul_media_player_proxy_get_can_seek (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanSeek");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean
nul_nul_media_player_proxy_get_can_control (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerProxy *proxy = NUL_NUL_MEDIA_PLAYER_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanControl");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
nul_nul_media_player_proxy_init (NulNulMediaPlayerProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = nul_nul_media_player_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, NUL_TYPE_NUL_MEDIA_PLAYER_PROXY, NulNulMediaPlayerProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), nul_nul_media_player_interface_info ());
}

static void
nul_nul_media_player_proxy_class_init (NulNulMediaPlayerProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = nul_nul_media_player_proxy_finalize;
  gobject_class->get_property = nul_nul_media_player_proxy_get_property;
  gobject_class->set_property = nul_nul_media_player_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = nul_nul_media_player_proxy_g_signal;
  proxy_class->g_properties_changed = nul_nul_media_player_proxy_g_properties_changed;

  nul_nul_media_player_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (NulNulMediaPlayerProxyPrivate));
#endif
}

static void
nul_nul_media_player_proxy_iface_init (NulNulMediaPlayerIface *iface)
{
  iface->get_playback_status = nul_nul_media_player_proxy_get_playback_status;
  iface->get_loop_status = nul_nul_media_player_proxy_get_loop_status;
  iface->get_rate = nul_nul_media_player_proxy_get_rate;
  iface->get_shuffle = nul_nul_media_player_proxy_get_shuffle;
  iface->get_metadata = nul_nul_media_player_proxy_get_metadata;
  iface->get_volume = nul_nul_media_player_proxy_get_volume;
  iface->get_position = nul_nul_media_player_proxy_get_position;
  iface->get_minimum_rate = nul_nul_media_player_proxy_get_minimum_rate;
  iface->get_maximum_rate = nul_nul_media_player_proxy_get_maximum_rate;
  iface->get_can_go_next = nul_nul_media_player_proxy_get_can_go_next;
  iface->get_can_go_previous = nul_nul_media_player_proxy_get_can_go_previous;
  iface->get_can_play = nul_nul_media_player_proxy_get_can_play;
  iface->get_can_pause = nul_nul_media_player_proxy_get_can_pause;
  iface->get_can_seek = nul_nul_media_player_proxy_get_can_seek;
  iface->get_can_control = nul_nul_media_player_proxy_get_can_control;
}

/**
 * nul_nul_media_player_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Player.top_of_page">org.mpris.MediaPlayer2.Player</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_nul_media_player_proxy_new_finish() to get the result of the operation.
 *
 * See nul_nul_media_player_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
nul_nul_media_player_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (NUL_TYPE_NUL_MEDIA_PLAYER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.Player", NULL);
}

/**
 * nul_nul_media_player_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_nul_media_player_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with nul_nul_media_player_proxy_new().
 *
 * Returns: (transfer full) (type NulNulMediaPlayerProxy): The constructed proxy object or %NULL if @error is set.
 */
NulNulMediaPlayer *
nul_nul_media_player_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return NUL_NUL_MEDIA_PLAYER (ret);
  else
    return NULL;
}

/**
 * nul_nul_media_player_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Player.top_of_page">org.mpris.MediaPlayer2.Player</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See nul_nul_media_player_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type NulNulMediaPlayerProxy): The constructed proxy object or %NULL if @error is set.
 */
NulNulMediaPlayer *
nul_nul_media_player_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (NUL_TYPE_NUL_MEDIA_PLAYER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.Player", NULL);
  if (ret != NULL)
    return NUL_NUL_MEDIA_PLAYER (ret);
  else
    return NULL;
}


/**
 * nul_nul_media_player_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like nul_nul_media_player_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_nul_media_player_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See nul_nul_media_player_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
nul_nul_media_player_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (NUL_TYPE_NUL_MEDIA_PLAYER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.Player", NULL);
}

/**
 * nul_nul_media_player_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_nul_media_player_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with nul_nul_media_player_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type NulNulMediaPlayerProxy): The constructed proxy object or %NULL if @error is set.
 */
NulNulMediaPlayer *
nul_nul_media_player_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return NUL_NUL_MEDIA_PLAYER (ret);
  else
    return NULL;
}

/**
 * nul_nul_media_player_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like nul_nul_media_player_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See nul_nul_media_player_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type NulNulMediaPlayerProxy): The constructed proxy object or %NULL if @error is set.
 */
NulNulMediaPlayer *
nul_nul_media_player_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (NUL_TYPE_NUL_MEDIA_PLAYER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.Player", NULL);
  if (ret != NULL)
    return NUL_NUL_MEDIA_PLAYER (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * NulNulMediaPlayerSkeleton:
 *
 * The #NulNulMediaPlayerSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * NulNulMediaPlayerSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #NulNulMediaPlayerSkeleton.
 */

struct _NulNulMediaPlayerSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_nul_nul_media_player_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], NUL_TYPE_NUL_MEDIA_PLAYER);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, NUL_TYPE_NUL_MEDIA_PLAYER);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_nul_nul_media_player_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_nul_media_player_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_nul_nul_media_player_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_nul_media_player_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _nul_nul_media_player_skeleton_vtable =
{
  _nul_nul_media_player_skeleton_handle_method_call,
  _nul_nul_media_player_skeleton_handle_get_property,
  _nul_nul_media_player_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
nul_nul_media_player_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return nul_nul_media_player_interface_info ();
}

static GDBusInterfaceVTable *
nul_nul_media_player_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_nul_nul_media_player_skeleton_vtable;
}

static GVariant *
nul_nul_media_player_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_nul_nul_media_player_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _nul_nul_media_player_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _nul_nul_media_player_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _nul_nul_media_player_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.mpris.MediaPlayer2.Player", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _nul_nul_media_player_emit_changed (gpointer user_data);

static void
nul_nul_media_player_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _nul_nul_media_player_emit_changed (skeleton);
}

static void
_nul_nul_media_player_on_signal_seeked (
    NulNulMediaPlayer *object,
    gint64 arg_Position)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(x)",
                   arg_Position));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.mpris.MediaPlayer2.Player", "Seeked",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void nul_nul_media_player_skeleton_iface_init (NulNulMediaPlayerIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (NulNulMediaPlayerSkeleton, nul_nul_media_player_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (NulNulMediaPlayerSkeleton)
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_NUL_MEDIA_PLAYER, nul_nul_media_player_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (NulNulMediaPlayerSkeleton, nul_nul_media_player_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_NUL_MEDIA_PLAYER, nul_nul_media_player_skeleton_iface_init));

#endif
static void
nul_nul_media_player_skeleton_finalize (GObject *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  guint n;
  for (n = 0; n < 15; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (nul_nul_media_player_skeleton_parent_class)->finalize (object);
}

static void
nul_nul_media_player_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 15);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_nul_nul_media_player_emit_changed (gpointer user_data)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.mpris.MediaPlayer2.Player",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_nul_nul_media_player_schedule_emit_changed (NulNulMediaPlayerSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
nul_nul_media_player_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _nul_nul_media_player_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
nul_nul_media_player_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 15);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _nul_nul_media_player_schedule_emit_changed (skeleton, _nul_nul_media_player_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
nul_nul_media_player_skeleton_init (NulNulMediaPlayerSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = nul_nul_media_player_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, NUL_TYPE_NUL_MEDIA_PLAYER_SKELETON, NulNulMediaPlayerSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 15);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_DOUBLE);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_VARIANT);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_DOUBLE);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_INT64);
  g_value_init (&skeleton->priv->properties[7], G_TYPE_DOUBLE);
  g_value_init (&skeleton->priv->properties[8], G_TYPE_DOUBLE);
  g_value_init (&skeleton->priv->properties[9], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[10], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[11], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[12], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[13], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[14], G_TYPE_BOOLEAN);
}

static const gchar *
nul_nul_media_player_skeleton_get_playback_status (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
nul_nul_media_player_skeleton_get_loop_status (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gdouble
nul_nul_media_player_skeleton_get_rate (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  gdouble value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_double (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean
nul_nul_media_player_skeleton_get_shuffle (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static GVariant *
nul_nul_media_player_skeleton_get_metadata (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  GVariant *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_variant (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gdouble
nul_nul_media_player_skeleton_get_volume (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  gdouble value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_double (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint64
nul_nul_media_player_skeleton_get_position (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  gint64 value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int64 (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gdouble
nul_nul_media_player_skeleton_get_minimum_rate (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  gdouble value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_double (&(skeleton->priv->properties[7]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gdouble
nul_nul_media_player_skeleton_get_maximum_rate (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  gdouble value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_double (&(skeleton->priv->properties[8]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean
nul_nul_media_player_skeleton_get_can_go_next (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[9]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean
nul_nul_media_player_skeleton_get_can_go_previous (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[10]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean
nul_nul_media_player_skeleton_get_can_play (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[11]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean
nul_nul_media_player_skeleton_get_can_pause (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[12]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean
nul_nul_media_player_skeleton_get_can_seek (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[13]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean
nul_nul_media_player_skeleton_get_can_control (NulNulMediaPlayer *object)
{
  NulNulMediaPlayerSkeleton *skeleton = NUL_NUL_MEDIA_PLAYER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[14]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
nul_nul_media_player_skeleton_class_init (NulNulMediaPlayerSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = nul_nul_media_player_skeleton_finalize;
  gobject_class->get_property = nul_nul_media_player_skeleton_get_property;
  gobject_class->set_property = nul_nul_media_player_skeleton_set_property;
  gobject_class->notify       = nul_nul_media_player_skeleton_notify;


  nul_nul_media_player_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = nul_nul_media_player_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = nul_nul_media_player_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = nul_nul_media_player_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = nul_nul_media_player_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (NulNulMediaPlayerSkeletonPrivate));
#endif
}

static void
nul_nul_media_player_skeleton_iface_init (NulNulMediaPlayerIface *iface)
{
  iface->seeked = _nul_nul_media_player_on_signal_seeked;
  iface->get_playback_status = nul_nul_media_player_skeleton_get_playback_status;
  iface->get_loop_status = nul_nul_media_player_skeleton_get_loop_status;
  iface->get_rate = nul_nul_media_player_skeleton_get_rate;
  iface->get_shuffle = nul_nul_media_player_skeleton_get_shuffle;
  iface->get_metadata = nul_nul_media_player_skeleton_get_metadata;
  iface->get_volume = nul_nul_media_player_skeleton_get_volume;
  iface->get_position = nul_nul_media_player_skeleton_get_position;
  iface->get_minimum_rate = nul_nul_media_player_skeleton_get_minimum_rate;
  iface->get_maximum_rate = nul_nul_media_player_skeleton_get_maximum_rate;
  iface->get_can_go_next = nul_nul_media_player_skeleton_get_can_go_next;
  iface->get_can_go_previous = nul_nul_media_player_skeleton_get_can_go_previous;
  iface->get_can_play = nul_nul_media_player_skeleton_get_can_play;
  iface->get_can_pause = nul_nul_media_player_skeleton_get_can_pause;
  iface->get_can_seek = nul_nul_media_player_skeleton_get_can_seek;
  iface->get_can_control = nul_nul_media_player_skeleton_get_can_control;
}

/**
 * nul_nul_media_player_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Player.top_of_page">org.mpris.MediaPlayer2.Player</link>.
 *
 * Returns: (transfer full) (type NulNulMediaPlayerSkeleton): The skeleton object.
 */
NulNulMediaPlayer *
nul_nul_media_player_skeleton_new (void)
{
  return NUL_NUL_MEDIA_PLAYER (g_object_new (NUL_TYPE_NUL_MEDIA_PLAYER_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.mpris.MediaPlayer2.Playlists
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:NulOrgMprisMediaPlayer2Playlists
 * @title: NulOrgMprisMediaPlayer2Playlists
 * @short_description: Generated C code for the org.mpris.MediaPlayer2.Playlists D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Playlists.top_of_page">org.mpris.MediaPlayer2.Playlists</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.mpris.MediaPlayer2.Playlists ---- */

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_playlists_method_info_activate_playlist_IN_ARG_PlaylistId =
{
  {
    -1,
    (gchar *) "PlaylistId",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_org_mpris_media_player2_playlists_method_info_activate_playlist_IN_ARG_pointers[] =
{
  &_nul_org_mpris_media_player2_playlists_method_info_activate_playlist_IN_ARG_PlaylistId,
  NULL
};

static const _ExtendedGDBusMethodInfo _nul_org_mpris_media_player2_playlists_method_info_activate_playlist =
{
  {
    -1,
    (gchar *) "ActivatePlaylist",
    (GDBusArgInfo **) &_nul_org_mpris_media_player2_playlists_method_info_activate_playlist_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-activate-playlist",
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_playlists_method_info_get_playlists_IN_ARG_Index =
{
  {
    -1,
    (gchar *) "Index",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_playlists_method_info_get_playlists_IN_ARG_MaxCount =
{
  {
    -1,
    (gchar *) "MaxCount",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_playlists_method_info_get_playlists_IN_ARG_Order =
{
  {
    -1,
    (gchar *) "Order",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_playlists_method_info_get_playlists_IN_ARG_ReverseOrder =
{
  {
    -1,
    (gchar *) "ReverseOrder",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_org_mpris_media_player2_playlists_method_info_get_playlists_IN_ARG_pointers[] =
{
  &_nul_org_mpris_media_player2_playlists_method_info_get_playlists_IN_ARG_Index,
  &_nul_org_mpris_media_player2_playlists_method_info_get_playlists_IN_ARG_MaxCount,
  &_nul_org_mpris_media_player2_playlists_method_info_get_playlists_IN_ARG_Order,
  &_nul_org_mpris_media_player2_playlists_method_info_get_playlists_IN_ARG_ReverseOrder,
  NULL
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_playlists_method_info_get_playlists_OUT_ARG_Playlists =
{
  {
    -1,
    (gchar *) "Playlists",
    (gchar *) "a(oss)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_org_mpris_media_player2_playlists_method_info_get_playlists_OUT_ARG_pointers[] =
{
  &_nul_org_mpris_media_player2_playlists_method_info_get_playlists_OUT_ARG_Playlists,
  NULL
};

static const _ExtendedGDBusMethodInfo _nul_org_mpris_media_player2_playlists_method_info_get_playlists =
{
  {
    -1,
    (gchar *) "GetPlaylists",
    (GDBusArgInfo **) &_nul_org_mpris_media_player2_playlists_method_info_get_playlists_IN_ARG_pointers,
    (GDBusArgInfo **) &_nul_org_mpris_media_player2_playlists_method_info_get_playlists_OUT_ARG_pointers,
    NULL
  },
  "handle-get-playlists",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _nul_org_mpris_media_player2_playlists_method_info_pointers[] =
{
  &_nul_org_mpris_media_player2_playlists_method_info_activate_playlist,
  &_nul_org_mpris_media_player2_playlists_method_info_get_playlists,
  NULL
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_playlists_signal_info_playlist_changed_ARG_Playlist =
{
  {
    -1,
    (gchar *) "Playlist",
    (gchar *) "(oss)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_org_mpris_media_player2_playlists_signal_info_playlist_changed_ARG_pointers[] =
{
  &_nul_org_mpris_media_player2_playlists_signal_info_playlist_changed_ARG_Playlist,
  NULL
};

static const _ExtendedGDBusSignalInfo _nul_org_mpris_media_player2_playlists_signal_info_playlist_changed =
{
  {
    -1,
    (gchar *) "PlaylistChanged",
    (GDBusArgInfo **) &_nul_org_mpris_media_player2_playlists_signal_info_playlist_changed_ARG_pointers,
    NULL
  },
  "playlist-changed"
};

static const _ExtendedGDBusSignalInfo * const _nul_org_mpris_media_player2_playlists_signal_info_pointers[] =
{
  &_nul_org_mpris_media_player2_playlists_signal_info_playlist_changed,
  NULL
};

static const GDBusAnnotationInfo _nul_org_mpris_media_player2_playlists_property_playlist_count_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_org_mpris_media_player2_playlists_property_playlist_count_annotation_info_pointers[] =
{
  &_nul_org_mpris_media_player2_playlists_property_playlist_count_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_org_mpris_media_player2_playlists_property_info_playlist_count =
{
  {
    -1,
    (gchar *) "PlaylistCount",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_org_mpris_media_player2_playlists_property_playlist_count_annotation_info_pointers
  },
  "playlist-count",
  FALSE
};

static const GDBusAnnotationInfo _nul_org_mpris_media_player2_playlists_property_orderings_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_org_mpris_media_player2_playlists_property_orderings_annotation_info_pointers[] =
{
  &_nul_org_mpris_media_player2_playlists_property_orderings_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_org_mpris_media_player2_playlists_property_info_orderings =
{
  {
    -1,
    (gchar *) "Orderings",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_org_mpris_media_player2_playlists_property_orderings_annotation_info_pointers
  },
  "orderings",
  FALSE
};

static const GDBusAnnotationInfo _nul_org_mpris_media_player2_playlists_property_active_playlist_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_org_mpris_media_player2_playlists_property_active_playlist_annotation_info_pointers[] =
{
  &_nul_org_mpris_media_player2_playlists_property_active_playlist_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_org_mpris_media_player2_playlists_property_info_active_playlist =
{
  {
    -1,
    (gchar *) "ActivePlaylist",
    (gchar *) "(b(oss))",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_org_mpris_media_player2_playlists_property_active_playlist_annotation_info_pointers
  },
  "active-playlist",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _nul_org_mpris_media_player2_playlists_property_info_pointers[] =
{
  &_nul_org_mpris_media_player2_playlists_property_info_playlist_count,
  &_nul_org_mpris_media_player2_playlists_property_info_orderings,
  &_nul_org_mpris_media_player2_playlists_property_info_active_playlist,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _nul_org_mpris_media_player2_playlists_interface_info =
{
  {
    -1,
    (gchar *) "org.mpris.MediaPlayer2.Playlists",
    (GDBusMethodInfo **) &_nul_org_mpris_media_player2_playlists_method_info_pointers,
    (GDBusSignalInfo **) &_nul_org_mpris_media_player2_playlists_signal_info_pointers,
    (GDBusPropertyInfo **) &_nul_org_mpris_media_player2_playlists_property_info_pointers,
    NULL
  },
  "org-mpris-media-player2-playlists",
};


/**
 * nul_org_mpris_media_player2_playlists_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Playlists.top_of_page">org.mpris.MediaPlayer2.Playlists</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
nul_org_mpris_media_player2_playlists_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_playlists_interface_info.parent_struct;
}

/**
 * nul_org_mpris_media_player2_playlists_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #NulOrgMprisMediaPlayer2Playlists interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
nul_org_mpris_media_player2_playlists_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "playlist-count");
  g_object_class_override_property (klass, property_id_begin++, "orderings");
  g_object_class_override_property (klass, property_id_begin++, "active-playlist");
  return property_id_begin - 1;
}



/**
 * NulOrgMprisMediaPlayer2Playlists:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Playlists.top_of_page">org.mpris.MediaPlayer2.Playlists</link>.
 */

/**
 * NulOrgMprisMediaPlayer2PlaylistsIface:
 * @parent_iface: The parent interface.
 * @handle_activate_playlist: Handler for the #NulOrgMprisMediaPlayer2Playlists::handle-activate-playlist signal.
 * @handle_get_playlists: Handler for the #NulOrgMprisMediaPlayer2Playlists::handle-get-playlists signal.
 * @get_active_playlist: Getter for the #NulOrgMprisMediaPlayer2Playlists:active-playlist property.
 * @get_orderings: Getter for the #NulOrgMprisMediaPlayer2Playlists:orderings property.
 * @get_playlist_count: Getter for the #NulOrgMprisMediaPlayer2Playlists:playlist-count property.
 * @playlist_changed: Handler for the #NulOrgMprisMediaPlayer2Playlists::playlist-changed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Playlists.top_of_page">org.mpris.MediaPlayer2.Playlists</link>.
 */

typedef NulOrgMprisMediaPlayer2PlaylistsIface NulOrgMprisMediaPlayer2PlaylistsInterface;
G_DEFINE_INTERFACE (NulOrgMprisMediaPlayer2Playlists, nul_org_mpris_media_player2_playlists, G_TYPE_OBJECT);

static void
nul_org_mpris_media_player2_playlists_default_init (NulOrgMprisMediaPlayer2PlaylistsIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * NulOrgMprisMediaPlayer2Playlists::handle-activate-playlist:
   * @object: A #NulOrgMprisMediaPlayer2Playlists.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_PlaylistId: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Playlists.ActivatePlaylist">ActivatePlaylist()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_org_mpris_media_player2_playlists_complete_activate_playlist() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-activate-playlist",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulOrgMprisMediaPlayer2PlaylistsIface, handle_activate_playlist),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * NulOrgMprisMediaPlayer2Playlists::handle-get-playlists:
   * @object: A #NulOrgMprisMediaPlayer2Playlists.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_Index: Argument passed by remote caller.
   * @arg_MaxCount: Argument passed by remote caller.
   * @arg_Order: Argument passed by remote caller.
   * @arg_ReverseOrder: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Playlists.GetPlaylists">GetPlaylists()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_org_mpris_media_player2_playlists_complete_get_playlists() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-playlists",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulOrgMprisMediaPlayer2PlaylistsIface, handle_get_playlists),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    5,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT, G_TYPE_UINT, G_TYPE_STRING, G_TYPE_BOOLEAN);

  /* GObject signals for received D-Bus signals: */
  /**
   * NulOrgMprisMediaPlayer2Playlists::playlist-changed:
   * @object: A #NulOrgMprisMediaPlayer2Playlists.
   * @arg_Playlist: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-mpris-MediaPlayer2-Playlists.PlaylistChanged">"PlaylistChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("playlist-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulOrgMprisMediaPlayer2PlaylistsIface, playlist_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_VARIANT);

  /* GObject properties for D-Bus properties: */
  /**
   * NulOrgMprisMediaPlayer2Playlists:playlist-count:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Playlists.PlaylistCount">"PlaylistCount"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("playlist-count", "PlaylistCount", "PlaylistCount", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulOrgMprisMediaPlayer2Playlists:orderings:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Playlists.Orderings">"Orderings"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("orderings", "Orderings", "Orderings", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulOrgMprisMediaPlayer2Playlists:active-playlist:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Playlists.ActivePlaylist">"ActivePlaylist"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_variant ("active-playlist", "ActivePlaylist", "ActivePlaylist", G_VARIANT_TYPE ("(b(oss))"), NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * nul_org_mpris_media_player2_playlists_get_playlist_count: (skip)
 * @object: A #NulOrgMprisMediaPlayer2Playlists.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Playlists.PlaylistCount">"PlaylistCount"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint
nul_org_mpris_media_player2_playlists_get_playlist_count (NulOrgMprisMediaPlayer2Playlists *object)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_GET_IFACE (object)->get_playlist_count (object);
}

/**
 * nul_org_mpris_media_player2_playlists_set_playlist_count: (skip)
 * @object: A #NulOrgMprisMediaPlayer2Playlists.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Playlists.PlaylistCount">"PlaylistCount"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_org_mpris_media_player2_playlists_set_playlist_count (NulOrgMprisMediaPlayer2Playlists *object, guint value)
{
  g_object_set (G_OBJECT (object), "playlist-count", value, NULL);
}

/**
 * nul_org_mpris_media_player2_playlists_get_orderings: (skip)
 * @object: A #NulOrgMprisMediaPlayer2Playlists.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Playlists.Orderings">"Orderings"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nul_org_mpris_media_player2_playlists_dup_orderings() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
nul_org_mpris_media_player2_playlists_get_orderings (NulOrgMprisMediaPlayer2Playlists *object)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_GET_IFACE (object)->get_orderings (object);
}

/**
 * nul_org_mpris_media_player2_playlists_dup_orderings: (skip)
 * @object: A #NulOrgMprisMediaPlayer2Playlists.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Playlists.Orderings">"Orderings"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
nul_org_mpris_media_player2_playlists_dup_orderings (NulOrgMprisMediaPlayer2Playlists *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "orderings", &value, NULL);
  return value;
}

/**
 * nul_org_mpris_media_player2_playlists_set_orderings: (skip)
 * @object: A #NulOrgMprisMediaPlayer2Playlists.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Playlists.Orderings">"Orderings"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_org_mpris_media_player2_playlists_set_orderings (NulOrgMprisMediaPlayer2Playlists *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "orderings", value, NULL);
}

/**
 * nul_org_mpris_media_player2_playlists_get_active_playlist: (skip)
 * @object: A #NulOrgMprisMediaPlayer2Playlists.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Playlists.ActivePlaylist">"ActivePlaylist"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nul_org_mpris_media_player2_playlists_dup_active_playlist() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
GVariant *
nul_org_mpris_media_player2_playlists_get_active_playlist (NulOrgMprisMediaPlayer2Playlists *object)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_GET_IFACE (object)->get_active_playlist (object);
}

/**
 * nul_org_mpris_media_player2_playlists_dup_active_playlist: (skip)
 * @object: A #NulOrgMprisMediaPlayer2Playlists.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Playlists.ActivePlaylist">"ActivePlaylist"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_variant_unref().
 */
GVariant *
nul_org_mpris_media_player2_playlists_dup_active_playlist (NulOrgMprisMediaPlayer2Playlists *object)
{
  GVariant *value;
  g_object_get (G_OBJECT (object), "active-playlist", &value, NULL);
  return value;
}

/**
 * nul_org_mpris_media_player2_playlists_set_active_playlist: (skip)
 * @object: A #NulOrgMprisMediaPlayer2Playlists.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Playlists.ActivePlaylist">"ActivePlaylist"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_org_mpris_media_player2_playlists_set_active_playlist (NulOrgMprisMediaPlayer2Playlists *object, GVariant *value)
{
  g_object_set (G_OBJECT (object), "active-playlist", value, NULL);
}

/**
 * nul_org_mpris_media_player2_playlists_emit_playlist_changed:
 * @object: A #NulOrgMprisMediaPlayer2Playlists.
 * @arg_Playlist: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-mpris-MediaPlayer2-Playlists.PlaylistChanged">"PlaylistChanged"</link> D-Bus signal.
 */
void
nul_org_mpris_media_player2_playlists_emit_playlist_changed (
    NulOrgMprisMediaPlayer2Playlists *object,
    GVariant *arg_Playlist)
{
  g_signal_emit_by_name (object, "playlist-changed", arg_Playlist);
}

/**
 * nul_org_mpris_media_player2_playlists_call_activate_playlist:
 * @proxy: A #NulOrgMprisMediaPlayer2PlaylistsProxy.
 * @arg_PlaylistId: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Playlists.ActivatePlaylist">ActivatePlaylist()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_org_mpris_media_player2_playlists_call_activate_playlist_finish() to get the result of the operation.
 *
 * See nul_org_mpris_media_player2_playlists_call_activate_playlist_sync() for the synchronous, blocking version of this method.
 */
void
nul_org_mpris_media_player2_playlists_call_activate_playlist (
    NulOrgMprisMediaPlayer2Playlists *proxy,
    const gchar *arg_PlaylistId,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ActivatePlaylist",
    g_variant_new ("(o)",
                   arg_PlaylistId),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_org_mpris_media_player2_playlists_call_activate_playlist_finish:
 * @proxy: A #NulOrgMprisMediaPlayer2PlaylistsProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_org_mpris_media_player2_playlists_call_activate_playlist().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_org_mpris_media_player2_playlists_call_activate_playlist().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_playlists_call_activate_playlist_finish (
    NulOrgMprisMediaPlayer2Playlists *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_playlists_call_activate_playlist_sync:
 * @proxy: A #NulOrgMprisMediaPlayer2PlaylistsProxy.
 * @arg_PlaylistId: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Playlists.ActivatePlaylist">ActivatePlaylist()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_org_mpris_media_player2_playlists_call_activate_playlist() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_playlists_call_activate_playlist_sync (
    NulOrgMprisMediaPlayer2Playlists *proxy,
    const gchar *arg_PlaylistId,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ActivatePlaylist",
    g_variant_new ("(o)",
                   arg_PlaylistId),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_playlists_call_get_playlists:
 * @proxy: A #NulOrgMprisMediaPlayer2PlaylistsProxy.
 * @arg_Index: Argument to pass with the method invocation.
 * @arg_MaxCount: Argument to pass with the method invocation.
 * @arg_Order: Argument to pass with the method invocation.
 * @arg_ReverseOrder: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Playlists.GetPlaylists">GetPlaylists()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_org_mpris_media_player2_playlists_call_get_playlists_finish() to get the result of the operation.
 *
 * See nul_org_mpris_media_player2_playlists_call_get_playlists_sync() for the synchronous, blocking version of this method.
 */
void
nul_org_mpris_media_player2_playlists_call_get_playlists (
    NulOrgMprisMediaPlayer2Playlists *proxy,
    guint arg_Index,
    guint arg_MaxCount,
    const gchar *arg_Order,
    gboolean arg_ReverseOrder,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetPlaylists",
    g_variant_new ("(uusb)",
                   arg_Index,
                   arg_MaxCount,
                   arg_Order,
                   arg_ReverseOrder),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_org_mpris_media_player2_playlists_call_get_playlists_finish:
 * @proxy: A #NulOrgMprisMediaPlayer2PlaylistsProxy.
 * @out_Playlists: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_org_mpris_media_player2_playlists_call_get_playlists().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_org_mpris_media_player2_playlists_call_get_playlists().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_playlists_call_get_playlists_finish (
    NulOrgMprisMediaPlayer2Playlists *proxy,
    GVariant **out_Playlists,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(oss))",
                 out_Playlists);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_playlists_call_get_playlists_sync:
 * @proxy: A #NulOrgMprisMediaPlayer2PlaylistsProxy.
 * @arg_Index: Argument to pass with the method invocation.
 * @arg_MaxCount: Argument to pass with the method invocation.
 * @arg_Order: Argument to pass with the method invocation.
 * @arg_ReverseOrder: Argument to pass with the method invocation.
 * @out_Playlists: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Playlists.GetPlaylists">GetPlaylists()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_org_mpris_media_player2_playlists_call_get_playlists() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_playlists_call_get_playlists_sync (
    NulOrgMprisMediaPlayer2Playlists *proxy,
    guint arg_Index,
    guint arg_MaxCount,
    const gchar *arg_Order,
    gboolean arg_ReverseOrder,
    GVariant **out_Playlists,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetPlaylists",
    g_variant_new ("(uusb)",
                   arg_Index,
                   arg_MaxCount,
                   arg_Order,
                   arg_ReverseOrder),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(oss))",
                 out_Playlists);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_playlists_complete_activate_playlist:
 * @object: A #NulOrgMprisMediaPlayer2Playlists.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Playlists.ActivatePlaylist">ActivatePlaylist()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_org_mpris_media_player2_playlists_complete_activate_playlist (
    NulOrgMprisMediaPlayer2Playlists *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * nul_org_mpris_media_player2_playlists_complete_get_playlists:
 * @object: A #NulOrgMprisMediaPlayer2Playlists.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @Playlists: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Playlists.GetPlaylists">GetPlaylists()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_org_mpris_media_player2_playlists_complete_get_playlists (
    NulOrgMprisMediaPlayer2Playlists *object,
    GDBusMethodInvocation *invocation,
    GVariant *Playlists)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a(oss))",
                   Playlists));
}

/* ------------------------------------------------------------------------ */

/**
 * NulOrgMprisMediaPlayer2PlaylistsProxy:
 *
 * The #NulOrgMprisMediaPlayer2PlaylistsProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * NulOrgMprisMediaPlayer2PlaylistsProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #NulOrgMprisMediaPlayer2PlaylistsProxy.
 */

struct _NulOrgMprisMediaPlayer2PlaylistsProxyPrivate
{
  GData *qdata;
};

static void nul_org_mpris_media_player2_playlists_proxy_iface_init (NulOrgMprisMediaPlayer2PlaylistsIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (NulOrgMprisMediaPlayer2PlaylistsProxy, nul_org_mpris_media_player2_playlists_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (NulOrgMprisMediaPlayer2PlaylistsProxy)
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS, nul_org_mpris_media_player2_playlists_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (NulOrgMprisMediaPlayer2PlaylistsProxy, nul_org_mpris_media_player2_playlists_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS, nul_org_mpris_media_player2_playlists_proxy_iface_init));

#endif
static void
nul_org_mpris_media_player2_playlists_proxy_finalize (GObject *object)
{
  NulOrgMprisMediaPlayer2PlaylistsProxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (nul_org_mpris_media_player2_playlists_proxy_parent_class)->finalize (object);
}

static void
nul_org_mpris_media_player2_playlists_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  info = _nul_org_mpris_media_player2_playlists_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
nul_org_mpris_media_player2_playlists_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.mpris.MediaPlayer2.Playlists: %s (%s, %d)",
                 info->parent_struct.name,
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
nul_org_mpris_media_player2_playlists_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  info = _nul_org_mpris_media_player2_playlists_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.mpris.MediaPlayer2.Playlists", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) nul_org_mpris_media_player2_playlists_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
nul_org_mpris_media_player2_playlists_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_playlists_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
nul_org_mpris_media_player2_playlists_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  NulOrgMprisMediaPlayer2PlaylistsProxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_playlists_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_playlists_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static guint
nul_org_mpris_media_player2_playlists_proxy_get_playlist_count (NulOrgMprisMediaPlayer2Playlists *object)
{
  NulOrgMprisMediaPlayer2PlaylistsProxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "PlaylistCount");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
nul_org_mpris_media_player2_playlists_proxy_get_orderings (NulOrgMprisMediaPlayer2Playlists *object)
{
  NulOrgMprisMediaPlayer2PlaylistsProxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "Orderings");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Orderings");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "Orderings", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static GVariant *
nul_org_mpris_media_player2_playlists_proxy_get_active_playlist (NulOrgMprisMediaPlayer2Playlists *object)
{
  NulOrgMprisMediaPlayer2PlaylistsProxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_PROXY (object);
  GVariant *variant;
  GVariant *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ActivePlaylist");
  value = variant;
  if (variant != NULL)
    g_variant_unref (variant);
  return value;
}

static void
nul_org_mpris_media_player2_playlists_proxy_init (NulOrgMprisMediaPlayer2PlaylistsProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = nul_org_mpris_media_player2_playlists_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_PROXY, NulOrgMprisMediaPlayer2PlaylistsProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), nul_org_mpris_media_player2_playlists_interface_info ());
}

static void
nul_org_mpris_media_player2_playlists_proxy_class_init (NulOrgMprisMediaPlayer2PlaylistsProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = nul_org_mpris_media_player2_playlists_proxy_finalize;
  gobject_class->get_property = nul_org_mpris_media_player2_playlists_proxy_get_property;
  gobject_class->set_property = nul_org_mpris_media_player2_playlists_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = nul_org_mpris_media_player2_playlists_proxy_g_signal;
  proxy_class->g_properties_changed = nul_org_mpris_media_player2_playlists_proxy_g_properties_changed;

  nul_org_mpris_media_player2_playlists_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (NulOrgMprisMediaPlayer2PlaylistsProxyPrivate));
#endif
}

static void
nul_org_mpris_media_player2_playlists_proxy_iface_init (NulOrgMprisMediaPlayer2PlaylistsIface *iface)
{
  iface->get_playlist_count = nul_org_mpris_media_player2_playlists_proxy_get_playlist_count;
  iface->get_orderings = nul_org_mpris_media_player2_playlists_proxy_get_orderings;
  iface->get_active_playlist = nul_org_mpris_media_player2_playlists_proxy_get_active_playlist;
}

/**
 * nul_org_mpris_media_player2_playlists_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Playlists.top_of_page">org.mpris.MediaPlayer2.Playlists</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_org_mpris_media_player2_playlists_proxy_new_finish() to get the result of the operation.
 *
 * See nul_org_mpris_media_player2_playlists_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
nul_org_mpris_media_player2_playlists_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.Playlists", NULL);
}

/**
 * nul_org_mpris_media_player2_playlists_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_org_mpris_media_player2_playlists_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with nul_org_mpris_media_player2_playlists_proxy_new().
 *
 * Returns: (transfer full) (type NulOrgMprisMediaPlayer2PlaylistsProxy): The constructed proxy object or %NULL if @error is set.
 */
NulOrgMprisMediaPlayer2Playlists *
nul_org_mpris_media_player2_playlists_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS (ret);
  else
    return NULL;
}

/**
 * nul_org_mpris_media_player2_playlists_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Playlists.top_of_page">org.mpris.MediaPlayer2.Playlists</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See nul_org_mpris_media_player2_playlists_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type NulOrgMprisMediaPlayer2PlaylistsProxy): The constructed proxy object or %NULL if @error is set.
 */
NulOrgMprisMediaPlayer2Playlists *
nul_org_mpris_media_player2_playlists_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.Playlists", NULL);
  if (ret != NULL)
    return NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS (ret);
  else
    return NULL;
}


/**
 * nul_org_mpris_media_player2_playlists_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like nul_org_mpris_media_player2_playlists_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_org_mpris_media_player2_playlists_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See nul_org_mpris_media_player2_playlists_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
nul_org_mpris_media_player2_playlists_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.Playlists", NULL);
}

/**
 * nul_org_mpris_media_player2_playlists_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_org_mpris_media_player2_playlists_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with nul_org_mpris_media_player2_playlists_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type NulOrgMprisMediaPlayer2PlaylistsProxy): The constructed proxy object or %NULL if @error is set.
 */
NulOrgMprisMediaPlayer2Playlists *
nul_org_mpris_media_player2_playlists_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS (ret);
  else
    return NULL;
}

/**
 * nul_org_mpris_media_player2_playlists_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like nul_org_mpris_media_player2_playlists_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See nul_org_mpris_media_player2_playlists_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type NulOrgMprisMediaPlayer2PlaylistsProxy): The constructed proxy object or %NULL if @error is set.
 */
NulOrgMprisMediaPlayer2Playlists *
nul_org_mpris_media_player2_playlists_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.Playlists", NULL);
  if (ret != NULL)
    return NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * NulOrgMprisMediaPlayer2PlaylistsSkeleton:
 *
 * The #NulOrgMprisMediaPlayer2PlaylistsSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * NulOrgMprisMediaPlayer2PlaylistsSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #NulOrgMprisMediaPlayer2PlaylistsSkeleton.
 */

struct _NulOrgMprisMediaPlayer2PlaylistsSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_nul_org_mpris_media_player2_playlists_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_nul_org_mpris_media_player2_playlists_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_playlists_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_nul_org_mpris_media_player2_playlists_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_playlists_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _nul_org_mpris_media_player2_playlists_skeleton_vtable =
{
  _nul_org_mpris_media_player2_playlists_skeleton_handle_method_call,
  _nul_org_mpris_media_player2_playlists_skeleton_handle_get_property,
  _nul_org_mpris_media_player2_playlists_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
nul_org_mpris_media_player2_playlists_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return nul_org_mpris_media_player2_playlists_interface_info ();
}

static GDBusInterfaceVTable *
nul_org_mpris_media_player2_playlists_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_nul_org_mpris_media_player2_playlists_skeleton_vtable;
}

static GVariant *
nul_org_mpris_media_player2_playlists_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_nul_org_mpris_media_player2_playlists_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _nul_org_mpris_media_player2_playlists_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _nul_org_mpris_media_player2_playlists_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _nul_org_mpris_media_player2_playlists_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.mpris.MediaPlayer2.Playlists", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _nul_org_mpris_media_player2_playlists_emit_changed (gpointer user_data);

static void
nul_org_mpris_media_player2_playlists_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _nul_org_mpris_media_player2_playlists_emit_changed (skeleton);
}

static void
_nul_org_mpris_media_player2_playlists_on_signal_playlist_changed (
    NulOrgMprisMediaPlayer2Playlists *object,
    GVariant *arg_Playlist)
{
  NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(@(oss))",
                   arg_Playlist));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.mpris.MediaPlayer2.Playlists", "PlaylistChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void nul_org_mpris_media_player2_playlists_skeleton_iface_init (NulOrgMprisMediaPlayer2PlaylistsIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (NulOrgMprisMediaPlayer2PlaylistsSkeleton, nul_org_mpris_media_player2_playlists_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (NulOrgMprisMediaPlayer2PlaylistsSkeleton)
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS, nul_org_mpris_media_player2_playlists_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (NulOrgMprisMediaPlayer2PlaylistsSkeleton, nul_org_mpris_media_player2_playlists_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS, nul_org_mpris_media_player2_playlists_skeleton_iface_init));

#endif
static void
nul_org_mpris_media_player2_playlists_skeleton_finalize (GObject *object)
{
  NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON (object);
  guint n;
  for (n = 0; n < 3; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (nul_org_mpris_media_player2_playlists_skeleton_parent_class)->finalize (object);
}

static void
nul_org_mpris_media_player2_playlists_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_nul_org_mpris_media_player2_playlists_emit_changed (gpointer user_data)
{
  NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.mpris.MediaPlayer2.Playlists",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_nul_org_mpris_media_player2_playlists_schedule_emit_changed (NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
nul_org_mpris_media_player2_playlists_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _nul_org_mpris_media_player2_playlists_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
nul_org_mpris_media_player2_playlists_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _nul_org_mpris_media_player2_playlists_schedule_emit_changed (skeleton, _nul_org_mpris_media_player2_playlists_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
nul_org_mpris_media_player2_playlists_skeleton_init (NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = nul_org_mpris_media_player2_playlists_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON, NulOrgMprisMediaPlayer2PlaylistsSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 3);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_VARIANT);
}

static guint
nul_org_mpris_media_player2_playlists_skeleton_get_playlist_count (NulOrgMprisMediaPlayer2Playlists *object)
{
  NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
nul_org_mpris_media_player2_playlists_skeleton_get_orderings (NulOrgMprisMediaPlayer2Playlists *object)
{
  NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static GVariant *
nul_org_mpris_media_player2_playlists_skeleton_get_active_playlist (NulOrgMprisMediaPlayer2Playlists *object)
{
  NulOrgMprisMediaPlayer2PlaylistsSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON (object);
  GVariant *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_variant (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
nul_org_mpris_media_player2_playlists_skeleton_class_init (NulOrgMprisMediaPlayer2PlaylistsSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = nul_org_mpris_media_player2_playlists_skeleton_finalize;
  gobject_class->get_property = nul_org_mpris_media_player2_playlists_skeleton_get_property;
  gobject_class->set_property = nul_org_mpris_media_player2_playlists_skeleton_set_property;
  gobject_class->notify       = nul_org_mpris_media_player2_playlists_skeleton_notify;


  nul_org_mpris_media_player2_playlists_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = nul_org_mpris_media_player2_playlists_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = nul_org_mpris_media_player2_playlists_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = nul_org_mpris_media_player2_playlists_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = nul_org_mpris_media_player2_playlists_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (NulOrgMprisMediaPlayer2PlaylistsSkeletonPrivate));
#endif
}

static void
nul_org_mpris_media_player2_playlists_skeleton_iface_init (NulOrgMprisMediaPlayer2PlaylistsIface *iface)
{
  iface->playlist_changed = _nul_org_mpris_media_player2_playlists_on_signal_playlist_changed;
  iface->get_playlist_count = nul_org_mpris_media_player2_playlists_skeleton_get_playlist_count;
  iface->get_orderings = nul_org_mpris_media_player2_playlists_skeleton_get_orderings;
  iface->get_active_playlist = nul_org_mpris_media_player2_playlists_skeleton_get_active_playlist;
}

/**
 * nul_org_mpris_media_player2_playlists_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Playlists.top_of_page">org.mpris.MediaPlayer2.Playlists</link>.
 *
 * Returns: (transfer full) (type NulOrgMprisMediaPlayer2PlaylistsSkeleton): The skeleton object.
 */
NulOrgMprisMediaPlayer2Playlists *
nul_org_mpris_media_player2_playlists_skeleton_new (void)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS (g_object_new (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PLAYLISTS_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.mpris.MediaPlayer2.TrackList
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:NulOrgMprisMediaPlayer2TrackList
 * @title: NulOrgMprisMediaPlayer2TrackList
 * @short_description: Generated C code for the org.mpris.MediaPlayer2.TrackList D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-mpris-MediaPlayer2-TrackList.top_of_page">org.mpris.MediaPlayer2.TrackList</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.mpris.MediaPlayer2.TrackList ---- */

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_track_list_method_info_get_tracks_metadata_IN_ARG_TrackIds =
{
  {
    -1,
    (gchar *) "TrackIds",
    (gchar *) "ao",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_org_mpris_media_player2_track_list_method_info_get_tracks_metadata_IN_ARG_pointers[] =
{
  &_nul_org_mpris_media_player2_track_list_method_info_get_tracks_metadata_IN_ARG_TrackIds,
  NULL
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_track_list_method_info_get_tracks_metadata_OUT_ARG_Metadata =
{
  {
    -1,
    (gchar *) "Metadata",
    (gchar *) "aa{sv}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_org_mpris_media_player2_track_list_method_info_get_tracks_metadata_OUT_ARG_pointers[] =
{
  &_nul_org_mpris_media_player2_track_list_method_info_get_tracks_metadata_OUT_ARG_Metadata,
  NULL
};

static const _ExtendedGDBusMethodInfo _nul_org_mpris_media_player2_track_list_method_info_get_tracks_metadata =
{
  {
    -1,
    (gchar *) "GetTracksMetadata",
    (GDBusArgInfo **) &_nul_org_mpris_media_player2_track_list_method_info_get_tracks_metadata_IN_ARG_pointers,
    (GDBusArgInfo **) &_nul_org_mpris_media_player2_track_list_method_info_get_tracks_metadata_OUT_ARG_pointers,
    NULL
  },
  "handle-get-tracks-metadata",
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_track_list_method_info_add_track_IN_ARG_Uri =
{
  {
    -1,
    (gchar *) "Uri",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_track_list_method_info_add_track_IN_ARG_AfterTrack =
{
  {
    -1,
    (gchar *) "AfterTrack",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_track_list_method_info_add_track_IN_ARG_SetAsCurrent =
{
  {
    -1,
    (gchar *) "SetAsCurrent",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_org_mpris_media_player2_track_list_method_info_add_track_IN_ARG_pointers[] =
{
  &_nul_org_mpris_media_player2_track_list_method_info_add_track_IN_ARG_Uri,
  &_nul_org_mpris_media_player2_track_list_method_info_add_track_IN_ARG_AfterTrack,
  &_nul_org_mpris_media_player2_track_list_method_info_add_track_IN_ARG_SetAsCurrent,
  NULL
};

static const _ExtendedGDBusMethodInfo _nul_org_mpris_media_player2_track_list_method_info_add_track =
{
  {
    -1,
    (gchar *) "AddTrack",
    (GDBusArgInfo **) &_nul_org_mpris_media_player2_track_list_method_info_add_track_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-add-track",
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_track_list_method_info_remove_track_IN_ARG_TrackId =
{
  {
    -1,
    (gchar *) "TrackId",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_org_mpris_media_player2_track_list_method_info_remove_track_IN_ARG_pointers[] =
{
  &_nul_org_mpris_media_player2_track_list_method_info_remove_track_IN_ARG_TrackId,
  NULL
};

static const _ExtendedGDBusMethodInfo _nul_org_mpris_media_player2_track_list_method_info_remove_track =
{
  {
    -1,
    (gchar *) "RemoveTrack",
    (GDBusArgInfo **) &_nul_org_mpris_media_player2_track_list_method_info_remove_track_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-remove-track",
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_track_list_method_info_go_to_IN_ARG_TrackId =
{
  {
    -1,
    (gchar *) "TrackId",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_org_mpris_media_player2_track_list_method_info_go_to_IN_ARG_pointers[] =
{
  &_nul_org_mpris_media_player2_track_list_method_info_go_to_IN_ARG_TrackId,
  NULL
};

static const _ExtendedGDBusMethodInfo _nul_org_mpris_media_player2_track_list_method_info_go_to =
{
  {
    -1,
    (gchar *) "GoTo",
    (GDBusArgInfo **) &_nul_org_mpris_media_player2_track_list_method_info_go_to_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-go-to",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _nul_org_mpris_media_player2_track_list_method_info_pointers[] =
{
  &_nul_org_mpris_media_player2_track_list_method_info_get_tracks_metadata,
  &_nul_org_mpris_media_player2_track_list_method_info_add_track,
  &_nul_org_mpris_media_player2_track_list_method_info_remove_track,
  &_nul_org_mpris_media_player2_track_list_method_info_go_to,
  NULL
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_track_list_signal_info_track_list_replaced_ARG_Tracks =
{
  {
    -1,
    (gchar *) "Tracks",
    (gchar *) "ao",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_track_list_signal_info_track_list_replaced_ARG_CurrentTrack =
{
  {
    -1,
    (gchar *) "CurrentTrack",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_org_mpris_media_player2_track_list_signal_info_track_list_replaced_ARG_pointers[] =
{
  &_nul_org_mpris_media_player2_track_list_signal_info_track_list_replaced_ARG_Tracks,
  &_nul_org_mpris_media_player2_track_list_signal_info_track_list_replaced_ARG_CurrentTrack,
  NULL
};

static const _ExtendedGDBusSignalInfo _nul_org_mpris_media_player2_track_list_signal_info_track_list_replaced =
{
  {
    -1,
    (gchar *) "TrackListReplaced",
    (GDBusArgInfo **) &_nul_org_mpris_media_player2_track_list_signal_info_track_list_replaced_ARG_pointers,
    NULL
  },
  "track-list-replaced"
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_track_list_signal_info_track_added_ARG_Metadata =
{
  {
    -1,
    (gchar *) "Metadata",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_track_list_signal_info_track_added_ARG_AfterTrack =
{
  {
    -1,
    (gchar *) "AfterTrack",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_org_mpris_media_player2_track_list_signal_info_track_added_ARG_pointers[] =
{
  &_nul_org_mpris_media_player2_track_list_signal_info_track_added_ARG_Metadata,
  &_nul_org_mpris_media_player2_track_list_signal_info_track_added_ARG_AfterTrack,
  NULL
};

static const _ExtendedGDBusSignalInfo _nul_org_mpris_media_player2_track_list_signal_info_track_added =
{
  {
    -1,
    (gchar *) "TrackAdded",
    (GDBusArgInfo **) &_nul_org_mpris_media_player2_track_list_signal_info_track_added_ARG_pointers,
    NULL
  },
  "track-added"
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_track_list_signal_info_track_removed_ARG_TrackId =
{
  {
    -1,
    (gchar *) "TrackId",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_org_mpris_media_player2_track_list_signal_info_track_removed_ARG_pointers[] =
{
  &_nul_org_mpris_media_player2_track_list_signal_info_track_removed_ARG_TrackId,
  NULL
};

static const _ExtendedGDBusSignalInfo _nul_org_mpris_media_player2_track_list_signal_info_track_removed =
{
  {
    -1,
    (gchar *) "TrackRemoved",
    (GDBusArgInfo **) &_nul_org_mpris_media_player2_track_list_signal_info_track_removed_ARG_pointers,
    NULL
  },
  "track-removed"
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_track_list_signal_info_track_metadata_changed_ARG_TrackId =
{
  {
    -1,
    (gchar *) "TrackId",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _nul_org_mpris_media_player2_track_list_signal_info_track_metadata_changed_ARG_Metadata =
{
  {
    -1,
    (gchar *) "Metadata",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _nul_org_mpris_media_player2_track_list_signal_info_track_metadata_changed_ARG_pointers[] =
{
  &_nul_org_mpris_media_player2_track_list_signal_info_track_metadata_changed_ARG_TrackId,
  &_nul_org_mpris_media_player2_track_list_signal_info_track_metadata_changed_ARG_Metadata,
  NULL
};

static const _ExtendedGDBusSignalInfo _nul_org_mpris_media_player2_track_list_signal_info_track_metadata_changed =
{
  {
    -1,
    (gchar *) "TrackMetadataChanged",
    (GDBusArgInfo **) &_nul_org_mpris_media_player2_track_list_signal_info_track_metadata_changed_ARG_pointers,
    NULL
  },
  "track-metadata-changed"
};

static const _ExtendedGDBusSignalInfo * const _nul_org_mpris_media_player2_track_list_signal_info_pointers[] =
{
  &_nul_org_mpris_media_player2_track_list_signal_info_track_list_replaced,
  &_nul_org_mpris_media_player2_track_list_signal_info_track_added,
  &_nul_org_mpris_media_player2_track_list_signal_info_track_removed,
  &_nul_org_mpris_media_player2_track_list_signal_info_track_metadata_changed,
  NULL
};

static const GDBusAnnotationInfo _nul_org_mpris_media_player2_track_list_property_tracks_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "invalidates",
  NULL
};

static const GDBusAnnotationInfo * const _nul_org_mpris_media_player2_track_list_property_tracks_annotation_info_pointers[] =
{
  &_nul_org_mpris_media_player2_track_list_property_tracks_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_org_mpris_media_player2_track_list_property_info_tracks =
{
  {
    -1,
    (gchar *) "Tracks",
    (gchar *) "ao",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_org_mpris_media_player2_track_list_property_tracks_annotation_info_pointers
  },
  "tracks",
  FALSE
};

static const GDBusAnnotationInfo _nul_org_mpris_media_player2_track_list_property_can_edit_tracks_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_org_mpris_media_player2_track_list_property_can_edit_tracks_annotation_info_pointers[] =
{
  &_nul_org_mpris_media_player2_track_list_property_can_edit_tracks_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_org_mpris_media_player2_track_list_property_info_can_edit_tracks =
{
  {
    -1,
    (gchar *) "CanEditTracks",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_org_mpris_media_player2_track_list_property_can_edit_tracks_annotation_info_pointers
  },
  "can-edit-tracks",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _nul_org_mpris_media_player2_track_list_property_info_pointers[] =
{
  &_nul_org_mpris_media_player2_track_list_property_info_tracks,
  &_nul_org_mpris_media_player2_track_list_property_info_can_edit_tracks,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _nul_org_mpris_media_player2_track_list_interface_info =
{
  {
    -1,
    (gchar *) "org.mpris.MediaPlayer2.TrackList",
    (GDBusMethodInfo **) &_nul_org_mpris_media_player2_track_list_method_info_pointers,
    (GDBusSignalInfo **) &_nul_org_mpris_media_player2_track_list_signal_info_pointers,
    (GDBusPropertyInfo **) &_nul_org_mpris_media_player2_track_list_property_info_pointers,
    NULL
  },
  "org-mpris-media-player2-track-list",
};


/**
 * nul_org_mpris_media_player2_track_list_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-mpris-MediaPlayer2-TrackList.top_of_page">org.mpris.MediaPlayer2.TrackList</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
nul_org_mpris_media_player2_track_list_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_track_list_interface_info.parent_struct;
}

/**
 * nul_org_mpris_media_player2_track_list_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #NulOrgMprisMediaPlayer2TrackList interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
nul_org_mpris_media_player2_track_list_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "tracks");
  g_object_class_override_property (klass, property_id_begin++, "can-edit-tracks");
  return property_id_begin - 1;
}



/**
 * NulOrgMprisMediaPlayer2TrackList:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-TrackList.top_of_page">org.mpris.MediaPlayer2.TrackList</link>.
 */

/**
 * NulOrgMprisMediaPlayer2TrackListIface:
 * @parent_iface: The parent interface.
 * @handle_add_track: Handler for the #NulOrgMprisMediaPlayer2TrackList::handle-add-track signal.
 * @handle_get_tracks_metadata: Handler for the #NulOrgMprisMediaPlayer2TrackList::handle-get-tracks-metadata signal.
 * @handle_go_to: Handler for the #NulOrgMprisMediaPlayer2TrackList::handle-go-to signal.
 * @handle_remove_track: Handler for the #NulOrgMprisMediaPlayer2TrackList::handle-remove-track signal.
 * @get_can_edit_tracks: Getter for the #NulOrgMprisMediaPlayer2TrackList:can-edit-tracks property.
 * @get_tracks: Getter for the #NulOrgMprisMediaPlayer2TrackList:tracks property.
 * @track_added: Handler for the #NulOrgMprisMediaPlayer2TrackList::track-added signal.
 * @track_list_replaced: Handler for the #NulOrgMprisMediaPlayer2TrackList::track-list-replaced signal.
 * @track_metadata_changed: Handler for the #NulOrgMprisMediaPlayer2TrackList::track-metadata-changed signal.
 * @track_removed: Handler for the #NulOrgMprisMediaPlayer2TrackList::track-removed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-TrackList.top_of_page">org.mpris.MediaPlayer2.TrackList</link>.
 */

typedef NulOrgMprisMediaPlayer2TrackListIface NulOrgMprisMediaPlayer2TrackListInterface;
G_DEFINE_INTERFACE (NulOrgMprisMediaPlayer2TrackList, nul_org_mpris_media_player2_track_list, G_TYPE_OBJECT);

static void
nul_org_mpris_media_player2_track_list_default_init (NulOrgMprisMediaPlayer2TrackListIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * NulOrgMprisMediaPlayer2TrackList::handle-get-tracks-metadata:
   * @object: A #NulOrgMprisMediaPlayer2TrackList.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_TrackIds: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.GetTracksMetadata">GetTracksMetadata()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_org_mpris_media_player2_track_list_complete_get_tracks_metadata() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-tracks-metadata",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulOrgMprisMediaPlayer2TrackListIface, handle_get_tracks_metadata),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRV);

  /**
   * NulOrgMprisMediaPlayer2TrackList::handle-add-track:
   * @object: A #NulOrgMprisMediaPlayer2TrackList.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_Uri: Argument passed by remote caller.
   * @arg_AfterTrack: Argument passed by remote caller.
   * @arg_SetAsCurrent: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.AddTrack">AddTrack()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_org_mpris_media_player2_track_list_complete_add_track() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-add-track",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulOrgMprisMediaPlayer2TrackListIface, handle_add_track),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_BOOLEAN);

  /**
   * NulOrgMprisMediaPlayer2TrackList::handle-remove-track:
   * @object: A #NulOrgMprisMediaPlayer2TrackList.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_TrackId: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.RemoveTrack">RemoveTrack()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_org_mpris_media_player2_track_list_complete_remove_track() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-remove-track",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulOrgMprisMediaPlayer2TrackListIface, handle_remove_track),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * NulOrgMprisMediaPlayer2TrackList::handle-go-to:
   * @object: A #NulOrgMprisMediaPlayer2TrackList.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_TrackId: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.GoTo">GoTo()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_org_mpris_media_player2_track_list_complete_go_to() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-go-to",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulOrgMprisMediaPlayer2TrackListIface, handle_go_to),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /* GObject signals for received D-Bus signals: */
  /**
   * NulOrgMprisMediaPlayer2TrackList::track-list-replaced:
   * @object: A #NulOrgMprisMediaPlayer2TrackList.
   * @arg_Tracks: Argument.
   * @arg_CurrentTrack: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-mpris-MediaPlayer2-TrackList.TrackListReplaced">"TrackListReplaced"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("track-list-replaced",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulOrgMprisMediaPlayer2TrackListIface, track_list_replaced),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_STRV, G_TYPE_STRING);

  /**
   * NulOrgMprisMediaPlayer2TrackList::track-added:
   * @object: A #NulOrgMprisMediaPlayer2TrackList.
   * @arg_Metadata: Argument.
   * @arg_AfterTrack: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-mpris-MediaPlayer2-TrackList.TrackAdded">"TrackAdded"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("track-added",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulOrgMprisMediaPlayer2TrackListIface, track_added),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_VARIANT, G_TYPE_STRING);

  /**
   * NulOrgMprisMediaPlayer2TrackList::track-removed:
   * @object: A #NulOrgMprisMediaPlayer2TrackList.
   * @arg_TrackId: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-mpris-MediaPlayer2-TrackList.TrackRemoved">"TrackRemoved"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("track-removed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulOrgMprisMediaPlayer2TrackListIface, track_removed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_STRING);

  /**
   * NulOrgMprisMediaPlayer2TrackList::track-metadata-changed:
   * @object: A #NulOrgMprisMediaPlayer2TrackList.
   * @arg_TrackId: Argument.
   * @arg_Metadata: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-mpris-MediaPlayer2-TrackList.TrackMetadataChanged">"TrackMetadataChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("track-metadata-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulOrgMprisMediaPlayer2TrackListIface, track_metadata_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_STRING, G_TYPE_VARIANT);

  /* GObject properties for D-Bus properties: */
  /**
   * NulOrgMprisMediaPlayer2TrackList:tracks:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-TrackList.Tracks">"Tracks"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("tracks", "Tracks", "Tracks", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulOrgMprisMediaPlayer2TrackList:can-edit-tracks:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-TrackList.CanEditTracks">"CanEditTracks"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-edit-tracks", "CanEditTracks", "CanEditTracks", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * nul_org_mpris_media_player2_track_list_get_tracks: (skip)
 * @object: A #NulOrgMprisMediaPlayer2TrackList.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-TrackList.Tracks">"Tracks"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nul_org_mpris_media_player2_track_list_dup_tracks() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
nul_org_mpris_media_player2_track_list_get_tracks (NulOrgMprisMediaPlayer2TrackList *object)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_GET_IFACE (object)->get_tracks (object);
}

/**
 * nul_org_mpris_media_player2_track_list_dup_tracks: (skip)
 * @object: A #NulOrgMprisMediaPlayer2TrackList.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-TrackList.Tracks">"Tracks"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
nul_org_mpris_media_player2_track_list_dup_tracks (NulOrgMprisMediaPlayer2TrackList *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "tracks", &value, NULL);
  return value;
}

/**
 * nul_org_mpris_media_player2_track_list_set_tracks: (skip)
 * @object: A #NulOrgMprisMediaPlayer2TrackList.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-TrackList.Tracks">"Tracks"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_org_mpris_media_player2_track_list_set_tracks (NulOrgMprisMediaPlayer2TrackList *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "tracks", value, NULL);
}

/**
 * nul_org_mpris_media_player2_track_list_get_can_edit_tracks: (skip)
 * @object: A #NulOrgMprisMediaPlayer2TrackList.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-TrackList.CanEditTracks">"CanEditTracks"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean
nul_org_mpris_media_player2_track_list_get_can_edit_tracks (NulOrgMprisMediaPlayer2TrackList *object)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_GET_IFACE (object)->get_can_edit_tracks (object);
}

/**
 * nul_org_mpris_media_player2_track_list_set_can_edit_tracks: (skip)
 * @object: A #NulOrgMprisMediaPlayer2TrackList.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-TrackList.CanEditTracks">"CanEditTracks"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_org_mpris_media_player2_track_list_set_can_edit_tracks (NulOrgMprisMediaPlayer2TrackList *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-edit-tracks", value, NULL);
}

/**
 * nul_org_mpris_media_player2_track_list_emit_track_list_replaced:
 * @object: A #NulOrgMprisMediaPlayer2TrackList.
 * @arg_Tracks: Argument to pass with the signal.
 * @arg_CurrentTrack: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-mpris-MediaPlayer2-TrackList.TrackListReplaced">"TrackListReplaced"</link> D-Bus signal.
 */
void
nul_org_mpris_media_player2_track_list_emit_track_list_replaced (
    NulOrgMprisMediaPlayer2TrackList *object,
    const gchar *const *arg_Tracks,
    const gchar *arg_CurrentTrack)
{
  g_signal_emit_by_name (object, "track-list-replaced", arg_Tracks, arg_CurrentTrack);
}

/**
 * nul_org_mpris_media_player2_track_list_emit_track_added:
 * @object: A #NulOrgMprisMediaPlayer2TrackList.
 * @arg_Metadata: Argument to pass with the signal.
 * @arg_AfterTrack: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-mpris-MediaPlayer2-TrackList.TrackAdded">"TrackAdded"</link> D-Bus signal.
 */
void
nul_org_mpris_media_player2_track_list_emit_track_added (
    NulOrgMprisMediaPlayer2TrackList *object,
    GVariant *arg_Metadata,
    const gchar *arg_AfterTrack)
{
  g_signal_emit_by_name (object, "track-added", arg_Metadata, arg_AfterTrack);
}

/**
 * nul_org_mpris_media_player2_track_list_emit_track_removed:
 * @object: A #NulOrgMprisMediaPlayer2TrackList.
 * @arg_TrackId: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-mpris-MediaPlayer2-TrackList.TrackRemoved">"TrackRemoved"</link> D-Bus signal.
 */
void
nul_org_mpris_media_player2_track_list_emit_track_removed (
    NulOrgMprisMediaPlayer2TrackList *object,
    const gchar *arg_TrackId)
{
  g_signal_emit_by_name (object, "track-removed", arg_TrackId);
}

/**
 * nul_org_mpris_media_player2_track_list_emit_track_metadata_changed:
 * @object: A #NulOrgMprisMediaPlayer2TrackList.
 * @arg_TrackId: Argument to pass with the signal.
 * @arg_Metadata: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-mpris-MediaPlayer2-TrackList.TrackMetadataChanged">"TrackMetadataChanged"</link> D-Bus signal.
 */
void
nul_org_mpris_media_player2_track_list_emit_track_metadata_changed (
    NulOrgMprisMediaPlayer2TrackList *object,
    const gchar *arg_TrackId,
    GVariant *arg_Metadata)
{
  g_signal_emit_by_name (object, "track-metadata-changed", arg_TrackId, arg_Metadata);
}

/**
 * nul_org_mpris_media_player2_track_list_call_get_tracks_metadata:
 * @proxy: A #NulOrgMprisMediaPlayer2TrackListProxy.
 * @arg_TrackIds: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.GetTracksMetadata">GetTracksMetadata()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_org_mpris_media_player2_track_list_call_get_tracks_metadata_finish() to get the result of the operation.
 *
 * See nul_org_mpris_media_player2_track_list_call_get_tracks_metadata_sync() for the synchronous, blocking version of this method.
 */
void
nul_org_mpris_media_player2_track_list_call_get_tracks_metadata (
    NulOrgMprisMediaPlayer2TrackList *proxy,
    const gchar *const *arg_TrackIds,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetTracksMetadata",
    g_variant_new ("(^ao)",
                   arg_TrackIds),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_org_mpris_media_player2_track_list_call_get_tracks_metadata_finish:
 * @proxy: A #NulOrgMprisMediaPlayer2TrackListProxy.
 * @out_Metadata: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_org_mpris_media_player2_track_list_call_get_tracks_metadata().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_org_mpris_media_player2_track_list_call_get_tracks_metadata().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_track_list_call_get_tracks_metadata_finish (
    NulOrgMprisMediaPlayer2TrackList *proxy,
    GVariant **out_Metadata,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@aa{sv})",
                 out_Metadata);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_track_list_call_get_tracks_metadata_sync:
 * @proxy: A #NulOrgMprisMediaPlayer2TrackListProxy.
 * @arg_TrackIds: Argument to pass with the method invocation.
 * @out_Metadata: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.GetTracksMetadata">GetTracksMetadata()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_org_mpris_media_player2_track_list_call_get_tracks_metadata() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_track_list_call_get_tracks_metadata_sync (
    NulOrgMprisMediaPlayer2TrackList *proxy,
    const gchar *const *arg_TrackIds,
    GVariant **out_Metadata,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetTracksMetadata",
    g_variant_new ("(^ao)",
                   arg_TrackIds),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@aa{sv})",
                 out_Metadata);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_track_list_call_add_track:
 * @proxy: A #NulOrgMprisMediaPlayer2TrackListProxy.
 * @arg_Uri: Argument to pass with the method invocation.
 * @arg_AfterTrack: Argument to pass with the method invocation.
 * @arg_SetAsCurrent: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.AddTrack">AddTrack()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_org_mpris_media_player2_track_list_call_add_track_finish() to get the result of the operation.
 *
 * See nul_org_mpris_media_player2_track_list_call_add_track_sync() for the synchronous, blocking version of this method.
 */
void
nul_org_mpris_media_player2_track_list_call_add_track (
    NulOrgMprisMediaPlayer2TrackList *proxy,
    const gchar *arg_Uri,
    const gchar *arg_AfterTrack,
    gboolean arg_SetAsCurrent,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "AddTrack",
    g_variant_new ("(sob)",
                   arg_Uri,
                   arg_AfterTrack,
                   arg_SetAsCurrent),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_org_mpris_media_player2_track_list_call_add_track_finish:
 * @proxy: A #NulOrgMprisMediaPlayer2TrackListProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_org_mpris_media_player2_track_list_call_add_track().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_org_mpris_media_player2_track_list_call_add_track().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_track_list_call_add_track_finish (
    NulOrgMprisMediaPlayer2TrackList *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_track_list_call_add_track_sync:
 * @proxy: A #NulOrgMprisMediaPlayer2TrackListProxy.
 * @arg_Uri: Argument to pass with the method invocation.
 * @arg_AfterTrack: Argument to pass with the method invocation.
 * @arg_SetAsCurrent: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.AddTrack">AddTrack()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_org_mpris_media_player2_track_list_call_add_track() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_track_list_call_add_track_sync (
    NulOrgMprisMediaPlayer2TrackList *proxy,
    const gchar *arg_Uri,
    const gchar *arg_AfterTrack,
    gboolean arg_SetAsCurrent,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "AddTrack",
    g_variant_new ("(sob)",
                   arg_Uri,
                   arg_AfterTrack,
                   arg_SetAsCurrent),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_track_list_call_remove_track:
 * @proxy: A #NulOrgMprisMediaPlayer2TrackListProxy.
 * @arg_TrackId: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.RemoveTrack">RemoveTrack()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_org_mpris_media_player2_track_list_call_remove_track_finish() to get the result of the operation.
 *
 * See nul_org_mpris_media_player2_track_list_call_remove_track_sync() for the synchronous, blocking version of this method.
 */
void
nul_org_mpris_media_player2_track_list_call_remove_track (
    NulOrgMprisMediaPlayer2TrackList *proxy,
    const gchar *arg_TrackId,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "RemoveTrack",
    g_variant_new ("(o)",
                   arg_TrackId),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_org_mpris_media_player2_track_list_call_remove_track_finish:
 * @proxy: A #NulOrgMprisMediaPlayer2TrackListProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_org_mpris_media_player2_track_list_call_remove_track().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_org_mpris_media_player2_track_list_call_remove_track().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_track_list_call_remove_track_finish (
    NulOrgMprisMediaPlayer2TrackList *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_track_list_call_remove_track_sync:
 * @proxy: A #NulOrgMprisMediaPlayer2TrackListProxy.
 * @arg_TrackId: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.RemoveTrack">RemoveTrack()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_org_mpris_media_player2_track_list_call_remove_track() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_track_list_call_remove_track_sync (
    NulOrgMprisMediaPlayer2TrackList *proxy,
    const gchar *arg_TrackId,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "RemoveTrack",
    g_variant_new ("(o)",
                   arg_TrackId),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_track_list_call_go_to:
 * @proxy: A #NulOrgMprisMediaPlayer2TrackListProxy.
 * @arg_TrackId: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.GoTo">GoTo()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_org_mpris_media_player2_track_list_call_go_to_finish() to get the result of the operation.
 *
 * See nul_org_mpris_media_player2_track_list_call_go_to_sync() for the synchronous, blocking version of this method.
 */
void
nul_org_mpris_media_player2_track_list_call_go_to (
    NulOrgMprisMediaPlayer2TrackList *proxy,
    const gchar *arg_TrackId,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GoTo",
    g_variant_new ("(o)",
                   arg_TrackId),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_org_mpris_media_player2_track_list_call_go_to_finish:
 * @proxy: A #NulOrgMprisMediaPlayer2TrackListProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_org_mpris_media_player2_track_list_call_go_to().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_org_mpris_media_player2_track_list_call_go_to().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_track_list_call_go_to_finish (
    NulOrgMprisMediaPlayer2TrackList *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_track_list_call_go_to_sync:
 * @proxy: A #NulOrgMprisMediaPlayer2TrackListProxy.
 * @arg_TrackId: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.GoTo">GoTo()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_org_mpris_media_player2_track_list_call_go_to() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_track_list_call_go_to_sync (
    NulOrgMprisMediaPlayer2TrackList *proxy,
    const gchar *arg_TrackId,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GoTo",
    g_variant_new ("(o)",
                   arg_TrackId),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_track_list_complete_get_tracks_metadata:
 * @object: A #NulOrgMprisMediaPlayer2TrackList.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @Metadata: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.GetTracksMetadata">GetTracksMetadata()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_org_mpris_media_player2_track_list_complete_get_tracks_metadata (
    NulOrgMprisMediaPlayer2TrackList *object,
    GDBusMethodInvocation *invocation,
    GVariant *Metadata)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@aa{sv})",
                   Metadata));
}

/**
 * nul_org_mpris_media_player2_track_list_complete_add_track:
 * @object: A #NulOrgMprisMediaPlayer2TrackList.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.AddTrack">AddTrack()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_org_mpris_media_player2_track_list_complete_add_track (
    NulOrgMprisMediaPlayer2TrackList *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * nul_org_mpris_media_player2_track_list_complete_remove_track:
 * @object: A #NulOrgMprisMediaPlayer2TrackList.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.RemoveTrack">RemoveTrack()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_org_mpris_media_player2_track_list_complete_remove_track (
    NulOrgMprisMediaPlayer2TrackList *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * nul_org_mpris_media_player2_track_list_complete_go_to:
 * @object: A #NulOrgMprisMediaPlayer2TrackList.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-TrackList.GoTo">GoTo()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_org_mpris_media_player2_track_list_complete_go_to (
    NulOrgMprisMediaPlayer2TrackList *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * NulOrgMprisMediaPlayer2TrackListProxy:
 *
 * The #NulOrgMprisMediaPlayer2TrackListProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * NulOrgMprisMediaPlayer2TrackListProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #NulOrgMprisMediaPlayer2TrackListProxy.
 */

struct _NulOrgMprisMediaPlayer2TrackListProxyPrivate
{
  GData *qdata;
};

static void nul_org_mpris_media_player2_track_list_proxy_iface_init (NulOrgMprisMediaPlayer2TrackListIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (NulOrgMprisMediaPlayer2TrackListProxy, nul_org_mpris_media_player2_track_list_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (NulOrgMprisMediaPlayer2TrackListProxy)
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST, nul_org_mpris_media_player2_track_list_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (NulOrgMprisMediaPlayer2TrackListProxy, nul_org_mpris_media_player2_track_list_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST, nul_org_mpris_media_player2_track_list_proxy_iface_init));

#endif
static void
nul_org_mpris_media_player2_track_list_proxy_finalize (GObject *object)
{
  NulOrgMprisMediaPlayer2TrackListProxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (nul_org_mpris_media_player2_track_list_proxy_parent_class)->finalize (object);
}

static void
nul_org_mpris_media_player2_track_list_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = _nul_org_mpris_media_player2_track_list_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
nul_org_mpris_media_player2_track_list_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.mpris.MediaPlayer2.TrackList: %s (%s, %d)",
                 info->parent_struct.name,
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
nul_org_mpris_media_player2_track_list_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = _nul_org_mpris_media_player2_track_list_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.mpris.MediaPlayer2.TrackList", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) nul_org_mpris_media_player2_track_list_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
nul_org_mpris_media_player2_track_list_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_track_list_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
nul_org_mpris_media_player2_track_list_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  NulOrgMprisMediaPlayer2TrackListProxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_track_list_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_track_list_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *const *
nul_org_mpris_media_player2_track_list_proxy_get_tracks (NulOrgMprisMediaPlayer2TrackList *object)
{
  NulOrgMprisMediaPlayer2TrackListProxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Tracks");
  if (variant != NULL)
    {
      value = g_variant_get_objv (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean
nul_org_mpris_media_player2_track_list_proxy_get_can_edit_tracks (NulOrgMprisMediaPlayer2TrackList *object)
{
  NulOrgMprisMediaPlayer2TrackListProxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanEditTracks");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
nul_org_mpris_media_player2_track_list_proxy_init (NulOrgMprisMediaPlayer2TrackListProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = nul_org_mpris_media_player2_track_list_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_PROXY, NulOrgMprisMediaPlayer2TrackListProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), nul_org_mpris_media_player2_track_list_interface_info ());
}

static void
nul_org_mpris_media_player2_track_list_proxy_class_init (NulOrgMprisMediaPlayer2TrackListProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = nul_org_mpris_media_player2_track_list_proxy_finalize;
  gobject_class->get_property = nul_org_mpris_media_player2_track_list_proxy_get_property;
  gobject_class->set_property = nul_org_mpris_media_player2_track_list_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = nul_org_mpris_media_player2_track_list_proxy_g_signal;
  proxy_class->g_properties_changed = nul_org_mpris_media_player2_track_list_proxy_g_properties_changed;

  nul_org_mpris_media_player2_track_list_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (NulOrgMprisMediaPlayer2TrackListProxyPrivate));
#endif
}

static void
nul_org_mpris_media_player2_track_list_proxy_iface_init (NulOrgMprisMediaPlayer2TrackListIface *iface)
{
  iface->get_tracks = nul_org_mpris_media_player2_track_list_proxy_get_tracks;
  iface->get_can_edit_tracks = nul_org_mpris_media_player2_track_list_proxy_get_can_edit_tracks;
}

/**
 * nul_org_mpris_media_player2_track_list_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-TrackList.top_of_page">org.mpris.MediaPlayer2.TrackList</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_org_mpris_media_player2_track_list_proxy_new_finish() to get the result of the operation.
 *
 * See nul_org_mpris_media_player2_track_list_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
nul_org_mpris_media_player2_track_list_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.TrackList", NULL);
}

/**
 * nul_org_mpris_media_player2_track_list_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_org_mpris_media_player2_track_list_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with nul_org_mpris_media_player2_track_list_proxy_new().
 *
 * Returns: (transfer full) (type NulOrgMprisMediaPlayer2TrackListProxy): The constructed proxy object or %NULL if @error is set.
 */
NulOrgMprisMediaPlayer2TrackList *
nul_org_mpris_media_player2_track_list_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST (ret);
  else
    return NULL;
}

/**
 * nul_org_mpris_media_player2_track_list_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-TrackList.top_of_page">org.mpris.MediaPlayer2.TrackList</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See nul_org_mpris_media_player2_track_list_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type NulOrgMprisMediaPlayer2TrackListProxy): The constructed proxy object or %NULL if @error is set.
 */
NulOrgMprisMediaPlayer2TrackList *
nul_org_mpris_media_player2_track_list_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.TrackList", NULL);
  if (ret != NULL)
    return NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST (ret);
  else
    return NULL;
}


/**
 * nul_org_mpris_media_player2_track_list_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like nul_org_mpris_media_player2_track_list_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_org_mpris_media_player2_track_list_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See nul_org_mpris_media_player2_track_list_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
nul_org_mpris_media_player2_track_list_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.TrackList", NULL);
}

/**
 * nul_org_mpris_media_player2_track_list_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_org_mpris_media_player2_track_list_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with nul_org_mpris_media_player2_track_list_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type NulOrgMprisMediaPlayer2TrackListProxy): The constructed proxy object or %NULL if @error is set.
 */
NulOrgMprisMediaPlayer2TrackList *
nul_org_mpris_media_player2_track_list_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST (ret);
  else
    return NULL;
}

/**
 * nul_org_mpris_media_player2_track_list_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like nul_org_mpris_media_player2_track_list_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See nul_org_mpris_media_player2_track_list_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type NulOrgMprisMediaPlayer2TrackListProxy): The constructed proxy object or %NULL if @error is set.
 */
NulOrgMprisMediaPlayer2TrackList *
nul_org_mpris_media_player2_track_list_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.TrackList", NULL);
  if (ret != NULL)
    return NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * NulOrgMprisMediaPlayer2TrackListSkeleton:
 *
 * The #NulOrgMprisMediaPlayer2TrackListSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * NulOrgMprisMediaPlayer2TrackListSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #NulOrgMprisMediaPlayer2TrackListSkeleton.
 */

struct _NulOrgMprisMediaPlayer2TrackListSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_nul_org_mpris_media_player2_track_list_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_nul_org_mpris_media_player2_track_list_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_track_list_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_nul_org_mpris_media_player2_track_list_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_track_list_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _nul_org_mpris_media_player2_track_list_skeleton_vtable =
{
  _nul_org_mpris_media_player2_track_list_skeleton_handle_method_call,
  _nul_org_mpris_media_player2_track_list_skeleton_handle_get_property,
  _nul_org_mpris_media_player2_track_list_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
nul_org_mpris_media_player2_track_list_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return nul_org_mpris_media_player2_track_list_interface_info ();
}

static GDBusInterfaceVTable *
nul_org_mpris_media_player2_track_list_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_nul_org_mpris_media_player2_track_list_skeleton_vtable;
}

static GVariant *
nul_org_mpris_media_player2_track_list_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_nul_org_mpris_media_player2_track_list_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _nul_org_mpris_media_player2_track_list_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _nul_org_mpris_media_player2_track_list_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _nul_org_mpris_media_player2_track_list_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.mpris.MediaPlayer2.TrackList", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _nul_org_mpris_media_player2_track_list_emit_changed (gpointer user_data);

static void
nul_org_mpris_media_player2_track_list_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _nul_org_mpris_media_player2_track_list_emit_changed (skeleton);
}

static void
_nul_org_mpris_media_player2_track_list_on_signal_track_list_replaced (
    NulOrgMprisMediaPlayer2TrackList *object,
    const gchar *const *arg_Tracks,
    const gchar *arg_CurrentTrack)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(^aoo)",
                   arg_Tracks,
                   arg_CurrentTrack));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.mpris.MediaPlayer2.TrackList", "TrackListReplaced",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_nul_org_mpris_media_player2_track_list_on_signal_track_added (
    NulOrgMprisMediaPlayer2TrackList *object,
    GVariant *arg_Metadata,
    const gchar *arg_AfterTrack)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(@a{sv}o)",
                   arg_Metadata,
                   arg_AfterTrack));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.mpris.MediaPlayer2.TrackList", "TrackAdded",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_nul_org_mpris_media_player2_track_list_on_signal_track_removed (
    NulOrgMprisMediaPlayer2TrackList *object,
    const gchar *arg_TrackId)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(o)",
                   arg_TrackId));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.mpris.MediaPlayer2.TrackList", "TrackRemoved",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_nul_org_mpris_media_player2_track_list_on_signal_track_metadata_changed (
    NulOrgMprisMediaPlayer2TrackList *object,
    const gchar *arg_TrackId,
    GVariant *arg_Metadata)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(o@a{sv})",
                   arg_TrackId,
                   arg_Metadata));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.mpris.MediaPlayer2.TrackList", "TrackMetadataChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void nul_org_mpris_media_player2_track_list_skeleton_iface_init (NulOrgMprisMediaPlayer2TrackListIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (NulOrgMprisMediaPlayer2TrackListSkeleton, nul_org_mpris_media_player2_track_list_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (NulOrgMprisMediaPlayer2TrackListSkeleton)
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST, nul_org_mpris_media_player2_track_list_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (NulOrgMprisMediaPlayer2TrackListSkeleton, nul_org_mpris_media_player2_track_list_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST, nul_org_mpris_media_player2_track_list_skeleton_iface_init));

#endif
static void
nul_org_mpris_media_player2_track_list_skeleton_finalize (GObject *object)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (object);
  guint n;
  for (n = 0; n < 2; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (nul_org_mpris_media_player2_track_list_skeleton_parent_class)->finalize (object);
}

static void
nul_org_mpris_media_player2_track_list_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_nul_org_mpris_media_player2_track_list_emit_changed (gpointer user_data)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.mpris.MediaPlayer2.TrackList",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_nul_org_mpris_media_player2_track_list_schedule_emit_changed (NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
nul_org_mpris_media_player2_track_list_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _nul_org_mpris_media_player2_track_list_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
nul_org_mpris_media_player2_track_list_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _nul_org_mpris_media_player2_track_list_schedule_emit_changed (skeleton, _nul_org_mpris_media_player2_track_list_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
nul_org_mpris_media_player2_track_list_skeleton_init (NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = nul_org_mpris_media_player2_track_list_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON, NulOrgMprisMediaPlayer2TrackListSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 2);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_BOOLEAN);
}

static const gchar *const *
nul_org_mpris_media_player2_track_list_skeleton_get_tracks (NulOrgMprisMediaPlayer2TrackList *object)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean
nul_org_mpris_media_player2_track_list_skeleton_get_can_edit_tracks (NulOrgMprisMediaPlayer2TrackList *object)
{
  NulOrgMprisMediaPlayer2TrackListSkeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
nul_org_mpris_media_player2_track_list_skeleton_class_init (NulOrgMprisMediaPlayer2TrackListSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = nul_org_mpris_media_player2_track_list_skeleton_finalize;
  gobject_class->get_property = nul_org_mpris_media_player2_track_list_skeleton_get_property;
  gobject_class->set_property = nul_org_mpris_media_player2_track_list_skeleton_set_property;
  gobject_class->notify       = nul_org_mpris_media_player2_track_list_skeleton_notify;


  nul_org_mpris_media_player2_track_list_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = nul_org_mpris_media_player2_track_list_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = nul_org_mpris_media_player2_track_list_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = nul_org_mpris_media_player2_track_list_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = nul_org_mpris_media_player2_track_list_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (NulOrgMprisMediaPlayer2TrackListSkeletonPrivate));
#endif
}

static void
nul_org_mpris_media_player2_track_list_skeleton_iface_init (NulOrgMprisMediaPlayer2TrackListIface *iface)
{
  iface->track_list_replaced = _nul_org_mpris_media_player2_track_list_on_signal_track_list_replaced;
  iface->track_added = _nul_org_mpris_media_player2_track_list_on_signal_track_added;
  iface->track_removed = _nul_org_mpris_media_player2_track_list_on_signal_track_removed;
  iface->track_metadata_changed = _nul_org_mpris_media_player2_track_list_on_signal_track_metadata_changed;
  iface->get_tracks = nul_org_mpris_media_player2_track_list_skeleton_get_tracks;
  iface->get_can_edit_tracks = nul_org_mpris_media_player2_track_list_skeleton_get_can_edit_tracks;
}

/**
 * nul_org_mpris_media_player2_track_list_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-TrackList.top_of_page">org.mpris.MediaPlayer2.TrackList</link>.
 *
 * Returns: (transfer full) (type NulOrgMprisMediaPlayer2TrackListSkeleton): The skeleton object.
 */
NulOrgMprisMediaPlayer2TrackList *
nul_org_mpris_media_player2_track_list_skeleton_new (void)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST (g_object_new (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_TRACK_LIST_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.mpris.MediaPlayer2
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:NulOrgMprisMediaPlayer2
 * @title: NulOrgMprisMediaPlayer2
 * @short_description: Generated C code for the org.mpris.MediaPlayer2 D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-mpris-MediaPlayer2.top_of_page">org.mpris.MediaPlayer2</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.mpris.MediaPlayer2 ---- */

static const _ExtendedGDBusMethodInfo _nul_org_mpris_media_player2_method_info_raise =
{
  {
    -1,
    (gchar *) "Raise",
    NULL,
    NULL,
    NULL
  },
  "handle-raise",
  FALSE
};

static const _ExtendedGDBusMethodInfo _nul_org_mpris_media_player2_method_info_quit =
{
  {
    -1,
    (gchar *) "Quit",
    NULL,
    NULL,
    NULL
  },
  "handle-quit",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _nul_org_mpris_media_player2_method_info_pointers[] =
{
  &_nul_org_mpris_media_player2_method_info_raise,
  &_nul_org_mpris_media_player2_method_info_quit,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_org_mpris_media_player2_property_info_can_quit =
{
  {
    -1,
    (gchar *) "CanQuit",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "can-quit",
  FALSE
};

static const GDBusAnnotationInfo _nul_org_mpris_media_player2_property_fullscreen_annotation_info_0 =
{
  -1,
  (gchar *) "org.mpris.MediaPlayer2.property.optional",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_org_mpris_media_player2_property_fullscreen_annotation_info_pointers[] =
{
  &_nul_org_mpris_media_player2_property_fullscreen_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_org_mpris_media_player2_property_info_fullscreen =
{
  {
    -1,
    (gchar *) "Fullscreen",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    (GDBusAnnotationInfo **) &_nul_org_mpris_media_player2_property_fullscreen_annotation_info_pointers
  },
  "fullscreen",
  FALSE
};

static const GDBusAnnotationInfo _nul_org_mpris_media_player2_property_can_set_fullscreen_annotation_info_0 =
{
  -1,
  (gchar *) "org.mpris.MediaPlayer2.property.optional",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_org_mpris_media_player2_property_can_set_fullscreen_annotation_info_pointers[] =
{
  &_nul_org_mpris_media_player2_property_can_set_fullscreen_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_org_mpris_media_player2_property_info_can_set_fullscreen =
{
  {
    -1,
    (gchar *) "CanSetFullscreen",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_org_mpris_media_player2_property_can_set_fullscreen_annotation_info_pointers
  },
  "can-set-fullscreen",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _nul_org_mpris_media_player2_property_info_can_raise =
{
  {
    -1,
    (gchar *) "CanRaise",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "can-raise",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _nul_org_mpris_media_player2_property_info_has_track_list =
{
  {
    -1,
    (gchar *) "HasTrackList",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "has-track-list",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _nul_org_mpris_media_player2_property_info_identity =
{
  {
    -1,
    (gchar *) "Identity",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "identity",
  FALSE
};

static const GDBusAnnotationInfo _nul_org_mpris_media_player2_property_desktop_entry_annotation_info_0 =
{
  -1,
  (gchar *) "org.mpris.MediaPlayer2.property.optional",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_org_mpris_media_player2_property_desktop_entry_annotation_info_pointers[] =
{
  &_nul_org_mpris_media_player2_property_desktop_entry_annotation_info_0,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nul_org_mpris_media_player2_property_info_desktop_entry =
{
  {
    -1,
    (gchar *) "DesktopEntry",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    (GDBusAnnotationInfo **) &_nul_org_mpris_media_player2_property_desktop_entry_annotation_info_pointers
  },
  "desktop-entry",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _nul_org_mpris_media_player2_property_info_supported_uri_schemes =
{
  {
    -1,
    (gchar *) "SupportedUriSchemes",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "supported-uri-schemes",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _nul_org_mpris_media_player2_property_info_supported_mime_types =
{
  {
    -1,
    (gchar *) "SupportedMimeTypes",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "supported-mime-types",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _nul_org_mpris_media_player2_property_info_pointers[] =
{
  &_nul_org_mpris_media_player2_property_info_can_quit,
  &_nul_org_mpris_media_player2_property_info_fullscreen,
  &_nul_org_mpris_media_player2_property_info_can_set_fullscreen,
  &_nul_org_mpris_media_player2_property_info_can_raise,
  &_nul_org_mpris_media_player2_property_info_has_track_list,
  &_nul_org_mpris_media_player2_property_info_identity,
  &_nul_org_mpris_media_player2_property_info_desktop_entry,
  &_nul_org_mpris_media_player2_property_info_supported_uri_schemes,
  &_nul_org_mpris_media_player2_property_info_supported_mime_types,
  NULL
};

static const GDBusAnnotationInfo _nul_org_mpris_media_player2_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.Property.EmitsChangedSignal",
  (gchar *) "true",
  NULL
};

static const GDBusAnnotationInfo * const _nul_org_mpris_media_player2_annotation_info_pointers[] =
{
  &_nul_org_mpris_media_player2_annotation_info_0,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _nul_org_mpris_media_player2_interface_info =
{
  {
    -1,
    (gchar *) "org.mpris.MediaPlayer2",
    (GDBusMethodInfo **) &_nul_org_mpris_media_player2_method_info_pointers,
    NULL,
    (GDBusPropertyInfo **) &_nul_org_mpris_media_player2_property_info_pointers,
    (GDBusAnnotationInfo **) &_nul_org_mpris_media_player2_annotation_info_pointers
  },
  "org-mpris-media-player2",
};


/**
 * nul_org_mpris_media_player2_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-mpris-MediaPlayer2.top_of_page">org.mpris.MediaPlayer2</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
nul_org_mpris_media_player2_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_interface_info.parent_struct;
}

/**
 * nul_org_mpris_media_player2_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #NulOrgMprisMediaPlayer2 interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
nul_org_mpris_media_player2_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "can-quit");
  g_object_class_override_property (klass, property_id_begin++, "fullscreen");
  g_object_class_override_property (klass, property_id_begin++, "can-set-fullscreen");
  g_object_class_override_property (klass, property_id_begin++, "can-raise");
  g_object_class_override_property (klass, property_id_begin++, "has-track-list");
  g_object_class_override_property (klass, property_id_begin++, "identity");
  g_object_class_override_property (klass, property_id_begin++, "desktop-entry");
  g_object_class_override_property (klass, property_id_begin++, "supported-uri-schemes");
  g_object_class_override_property (klass, property_id_begin++, "supported-mime-types");
  return property_id_begin - 1;
}



/**
 * NulOrgMprisMediaPlayer2:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2.top_of_page">org.mpris.MediaPlayer2</link>.
 */

/**
 * NulOrgMprisMediaPlayer2Iface:
 * @parent_iface: The parent interface.
 * @handle_quit: Handler for the #NulOrgMprisMediaPlayer2::handle-quit signal.
 * @handle_raise: Handler for the #NulOrgMprisMediaPlayer2::handle-raise signal.
 * @get_can_quit: Getter for the #NulOrgMprisMediaPlayer2:can-quit property.
 * @get_can_raise: Getter for the #NulOrgMprisMediaPlayer2:can-raise property.
 * @get_can_set_fullscreen: Getter for the #NulOrgMprisMediaPlayer2:can-set-fullscreen property.
 * @get_desktop_entry: Getter for the #NulOrgMprisMediaPlayer2:desktop-entry property.
 * @get_fullscreen: Getter for the #NulOrgMprisMediaPlayer2:fullscreen property.
 * @get_has_track_list: Getter for the #NulOrgMprisMediaPlayer2:has-track-list property.
 * @get_identity: Getter for the #NulOrgMprisMediaPlayer2:identity property.
 * @get_supported_mime_types: Getter for the #NulOrgMprisMediaPlayer2:supported-mime-types property.
 * @get_supported_uri_schemes: Getter for the #NulOrgMprisMediaPlayer2:supported-uri-schemes property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2.top_of_page">org.mpris.MediaPlayer2</link>.
 */

typedef NulOrgMprisMediaPlayer2Iface NulOrgMprisMediaPlayer2Interface;
G_DEFINE_INTERFACE (NulOrgMprisMediaPlayer2, nul_org_mpris_media_player2, G_TYPE_OBJECT);

static void
nul_org_mpris_media_player2_default_init (NulOrgMprisMediaPlayer2Iface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * NulOrgMprisMediaPlayer2::handle-raise:
   * @object: A #NulOrgMprisMediaPlayer2.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Raise">Raise()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_org_mpris_media_player2_complete_raise() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-raise",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulOrgMprisMediaPlayer2Iface, handle_raise),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * NulOrgMprisMediaPlayer2::handle-quit:
   * @object: A #NulOrgMprisMediaPlayer2.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Quit">Quit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nul_org_mpris_media_player2_complete_quit() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-quit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NulOrgMprisMediaPlayer2Iface, handle_quit),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject properties for D-Bus properties: */
  /**
   * NulOrgMprisMediaPlayer2:can-quit:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanQuit">"CanQuit"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-quit", "CanQuit", "CanQuit", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulOrgMprisMediaPlayer2:fullscreen:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.Fullscreen">"Fullscreen"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("fullscreen", "Fullscreen", "Fullscreen", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulOrgMprisMediaPlayer2:can-set-fullscreen:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanSetFullscreen">"CanSetFullscreen"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-set-fullscreen", "CanSetFullscreen", "CanSetFullscreen", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulOrgMprisMediaPlayer2:can-raise:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanRaise">"CanRaise"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-raise", "CanRaise", "CanRaise", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulOrgMprisMediaPlayer2:has-track-list:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.HasTrackList">"HasTrackList"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("has-track-list", "HasTrackList", "HasTrackList", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulOrgMprisMediaPlayer2:identity:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.Identity">"Identity"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("identity", "Identity", "Identity", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulOrgMprisMediaPlayer2:desktop-entry:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.DesktopEntry">"DesktopEntry"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("desktop-entry", "DesktopEntry", "DesktopEntry", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulOrgMprisMediaPlayer2:supported-uri-schemes:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedUriSchemes">"SupportedUriSchemes"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("supported-uri-schemes", "SupportedUriSchemes", "SupportedUriSchemes", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NulOrgMprisMediaPlayer2:supported-mime-types:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedMimeTypes">"SupportedMimeTypes"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("supported-mime-types", "SupportedMimeTypes", "SupportedMimeTypes", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * nul_org_mpris_media_player2_get_can_quit: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanQuit">"CanQuit"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean
nul_org_mpris_media_player2_get_can_quit (NulOrgMprisMediaPlayer2 *object)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_GET_IFACE (object)->get_can_quit (object);
}

/**
 * nul_org_mpris_media_player2_set_can_quit: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanQuit">"CanQuit"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_org_mpris_media_player2_set_can_quit (NulOrgMprisMediaPlayer2 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-quit", value, NULL);
}

/**
 * nul_org_mpris_media_player2_get_fullscreen: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.Fullscreen">"Fullscreen"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean
nul_org_mpris_media_player2_get_fullscreen (NulOrgMprisMediaPlayer2 *object)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_GET_IFACE (object)->get_fullscreen (object);
}

/**
 * nul_org_mpris_media_player2_set_fullscreen: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.Fullscreen">"Fullscreen"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
nul_org_mpris_media_player2_set_fullscreen (NulOrgMprisMediaPlayer2 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "fullscreen", value, NULL);
}

/**
 * nul_org_mpris_media_player2_get_can_set_fullscreen: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanSetFullscreen">"CanSetFullscreen"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean
nul_org_mpris_media_player2_get_can_set_fullscreen (NulOrgMprisMediaPlayer2 *object)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_GET_IFACE (object)->get_can_set_fullscreen (object);
}

/**
 * nul_org_mpris_media_player2_set_can_set_fullscreen: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanSetFullscreen">"CanSetFullscreen"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_org_mpris_media_player2_set_can_set_fullscreen (NulOrgMprisMediaPlayer2 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-set-fullscreen", value, NULL);
}

/**
 * nul_org_mpris_media_player2_get_can_raise: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanRaise">"CanRaise"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean
nul_org_mpris_media_player2_get_can_raise (NulOrgMprisMediaPlayer2 *object)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_GET_IFACE (object)->get_can_raise (object);
}

/**
 * nul_org_mpris_media_player2_set_can_raise: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanRaise">"CanRaise"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_org_mpris_media_player2_set_can_raise (NulOrgMprisMediaPlayer2 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-raise", value, NULL);
}

/**
 * nul_org_mpris_media_player2_get_has_track_list: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.HasTrackList">"HasTrackList"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean
nul_org_mpris_media_player2_get_has_track_list (NulOrgMprisMediaPlayer2 *object)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_GET_IFACE (object)->get_has_track_list (object);
}

/**
 * nul_org_mpris_media_player2_set_has_track_list: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.HasTrackList">"HasTrackList"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_org_mpris_media_player2_set_has_track_list (NulOrgMprisMediaPlayer2 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "has-track-list", value, NULL);
}

/**
 * nul_org_mpris_media_player2_get_identity: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.Identity">"Identity"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nul_org_mpris_media_player2_dup_identity() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nul_org_mpris_media_player2_get_identity (NulOrgMprisMediaPlayer2 *object)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_GET_IFACE (object)->get_identity (object);
}

/**
 * nul_org_mpris_media_player2_dup_identity: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.Identity">"Identity"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nul_org_mpris_media_player2_dup_identity (NulOrgMprisMediaPlayer2 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "identity", &value, NULL);
  return value;
}

/**
 * nul_org_mpris_media_player2_set_identity: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.Identity">"Identity"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_org_mpris_media_player2_set_identity (NulOrgMprisMediaPlayer2 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "identity", value, NULL);
}

/**
 * nul_org_mpris_media_player2_get_desktop_entry: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.DesktopEntry">"DesktopEntry"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nul_org_mpris_media_player2_dup_desktop_entry() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nul_org_mpris_media_player2_get_desktop_entry (NulOrgMprisMediaPlayer2 *object)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_GET_IFACE (object)->get_desktop_entry (object);
}

/**
 * nul_org_mpris_media_player2_dup_desktop_entry: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.DesktopEntry">"DesktopEntry"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nul_org_mpris_media_player2_dup_desktop_entry (NulOrgMprisMediaPlayer2 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "desktop-entry", &value, NULL);
  return value;
}

/**
 * nul_org_mpris_media_player2_set_desktop_entry: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.DesktopEntry">"DesktopEntry"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_org_mpris_media_player2_set_desktop_entry (NulOrgMprisMediaPlayer2 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "desktop-entry", value, NULL);
}

/**
 * nul_org_mpris_media_player2_get_supported_uri_schemes: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedUriSchemes">"SupportedUriSchemes"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nul_org_mpris_media_player2_dup_supported_uri_schemes() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
nul_org_mpris_media_player2_get_supported_uri_schemes (NulOrgMprisMediaPlayer2 *object)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_GET_IFACE (object)->get_supported_uri_schemes (object);
}

/**
 * nul_org_mpris_media_player2_dup_supported_uri_schemes: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedUriSchemes">"SupportedUriSchemes"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
nul_org_mpris_media_player2_dup_supported_uri_schemes (NulOrgMprisMediaPlayer2 *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "supported-uri-schemes", &value, NULL);
  return value;
}

/**
 * nul_org_mpris_media_player2_set_supported_uri_schemes: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedUriSchemes">"SupportedUriSchemes"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_org_mpris_media_player2_set_supported_uri_schemes (NulOrgMprisMediaPlayer2 *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "supported-uri-schemes", value, NULL);
}

/**
 * nul_org_mpris_media_player2_get_supported_mime_types: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedMimeTypes">"SupportedMimeTypes"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nul_org_mpris_media_player2_dup_supported_mime_types() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
nul_org_mpris_media_player2_get_supported_mime_types (NulOrgMprisMediaPlayer2 *object)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2_GET_IFACE (object)->get_supported_mime_types (object);
}

/**
 * nul_org_mpris_media_player2_dup_supported_mime_types: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedMimeTypes">"SupportedMimeTypes"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
nul_org_mpris_media_player2_dup_supported_mime_types (NulOrgMprisMediaPlayer2 *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "supported-mime-types", &value, NULL);
  return value;
}

/**
 * nul_org_mpris_media_player2_set_supported_mime_types: (skip)
 * @object: A #NulOrgMprisMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedMimeTypes">"SupportedMimeTypes"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nul_org_mpris_media_player2_set_supported_mime_types (NulOrgMprisMediaPlayer2 *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "supported-mime-types", value, NULL);
}

/**
 * nul_org_mpris_media_player2_call_raise:
 * @proxy: A #NulOrgMprisMediaPlayer2Proxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Raise">Raise()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_org_mpris_media_player2_call_raise_finish() to get the result of the operation.
 *
 * See nul_org_mpris_media_player2_call_raise_sync() for the synchronous, blocking version of this method.
 */
void
nul_org_mpris_media_player2_call_raise (
    NulOrgMprisMediaPlayer2 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Raise",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_org_mpris_media_player2_call_raise_finish:
 * @proxy: A #NulOrgMprisMediaPlayer2Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_org_mpris_media_player2_call_raise().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_org_mpris_media_player2_call_raise().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_call_raise_finish (
    NulOrgMprisMediaPlayer2 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_call_raise_sync:
 * @proxy: A #NulOrgMprisMediaPlayer2Proxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Raise">Raise()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_org_mpris_media_player2_call_raise() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_call_raise_sync (
    NulOrgMprisMediaPlayer2 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Raise",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_call_quit:
 * @proxy: A #NulOrgMprisMediaPlayer2Proxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Quit">Quit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_org_mpris_media_player2_call_quit_finish() to get the result of the operation.
 *
 * See nul_org_mpris_media_player2_call_quit_sync() for the synchronous, blocking version of this method.
 */
void
nul_org_mpris_media_player2_call_quit (
    NulOrgMprisMediaPlayer2 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Quit",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nul_org_mpris_media_player2_call_quit_finish:
 * @proxy: A #NulOrgMprisMediaPlayer2Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_org_mpris_media_player2_call_quit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nul_org_mpris_media_player2_call_quit().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_call_quit_finish (
    NulOrgMprisMediaPlayer2 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_call_quit_sync:
 * @proxy: A #NulOrgMprisMediaPlayer2Proxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Quit">Quit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nul_org_mpris_media_player2_call_quit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nul_org_mpris_media_player2_call_quit_sync (
    NulOrgMprisMediaPlayer2 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Quit",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nul_org_mpris_media_player2_complete_raise:
 * @object: A #NulOrgMprisMediaPlayer2.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Raise">Raise()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_org_mpris_media_player2_complete_raise (
    NulOrgMprisMediaPlayer2 *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * nul_org_mpris_media_player2_complete_quit:
 * @object: A #NulOrgMprisMediaPlayer2.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Quit">Quit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nul_org_mpris_media_player2_complete_quit (
    NulOrgMprisMediaPlayer2 *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * NulOrgMprisMediaPlayer2Proxy:
 *
 * The #NulOrgMprisMediaPlayer2Proxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * NulOrgMprisMediaPlayer2ProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #NulOrgMprisMediaPlayer2Proxy.
 */

struct _NulOrgMprisMediaPlayer2ProxyPrivate
{
  GData *qdata;
};

static void nul_org_mpris_media_player2_proxy_iface_init (NulOrgMprisMediaPlayer2Iface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (NulOrgMprisMediaPlayer2Proxy, nul_org_mpris_media_player2_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (NulOrgMprisMediaPlayer2Proxy)
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2, nul_org_mpris_media_player2_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (NulOrgMprisMediaPlayer2Proxy, nul_org_mpris_media_player2_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2, nul_org_mpris_media_player2_proxy_iface_init));

#endif
static void
nul_org_mpris_media_player2_proxy_finalize (GObject *object)
{
  NulOrgMprisMediaPlayer2Proxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (nul_org_mpris_media_player2_proxy_parent_class)->finalize (object);
}

static void
nul_org_mpris_media_player2_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 9);
  info = _nul_org_mpris_media_player2_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
nul_org_mpris_media_player2_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.mpris.MediaPlayer2: %s (%s, %d)",
                 info->parent_struct.name,
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
nul_org_mpris_media_player2_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 9);
  info = _nul_org_mpris_media_player2_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.mpris.MediaPlayer2", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) nul_org_mpris_media_player2_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
nul_org_mpris_media_player2_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
nul_org_mpris_media_player2_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  NulOrgMprisMediaPlayer2Proxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static gboolean
nul_org_mpris_media_player2_proxy_get_can_quit (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Proxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanQuit");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean
nul_org_mpris_media_player2_proxy_get_fullscreen (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Proxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Fullscreen");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean
nul_org_mpris_media_player2_proxy_get_can_set_fullscreen (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Proxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanSetFullscreen");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean
nul_org_mpris_media_player2_proxy_get_can_raise (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Proxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanRaise");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean
nul_org_mpris_media_player2_proxy_get_has_track_list (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Proxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "HasTrackList");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
nul_org_mpris_media_player2_proxy_get_identity (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Proxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Identity");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
nul_org_mpris_media_player2_proxy_get_desktop_entry (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Proxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "DesktopEntry");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
nul_org_mpris_media_player2_proxy_get_supported_uri_schemes (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Proxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "SupportedUriSchemes");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "SupportedUriSchemes");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "SupportedUriSchemes", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
nul_org_mpris_media_player2_proxy_get_supported_mime_types (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Proxy *proxy = NUL_ORG_MPRIS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "SupportedMimeTypes");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "SupportedMimeTypes");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "SupportedMimeTypes", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static void
nul_org_mpris_media_player2_proxy_init (NulOrgMprisMediaPlayer2Proxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = nul_org_mpris_media_player2_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PROXY, NulOrgMprisMediaPlayer2ProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), nul_org_mpris_media_player2_interface_info ());
}

static void
nul_org_mpris_media_player2_proxy_class_init (NulOrgMprisMediaPlayer2ProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = nul_org_mpris_media_player2_proxy_finalize;
  gobject_class->get_property = nul_org_mpris_media_player2_proxy_get_property;
  gobject_class->set_property = nul_org_mpris_media_player2_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = nul_org_mpris_media_player2_proxy_g_signal;
  proxy_class->g_properties_changed = nul_org_mpris_media_player2_proxy_g_properties_changed;

  nul_org_mpris_media_player2_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (NulOrgMprisMediaPlayer2ProxyPrivate));
#endif
}

static void
nul_org_mpris_media_player2_proxy_iface_init (NulOrgMprisMediaPlayer2Iface *iface)
{
  iface->get_can_quit = nul_org_mpris_media_player2_proxy_get_can_quit;
  iface->get_fullscreen = nul_org_mpris_media_player2_proxy_get_fullscreen;
  iface->get_can_set_fullscreen = nul_org_mpris_media_player2_proxy_get_can_set_fullscreen;
  iface->get_can_raise = nul_org_mpris_media_player2_proxy_get_can_raise;
  iface->get_has_track_list = nul_org_mpris_media_player2_proxy_get_has_track_list;
  iface->get_identity = nul_org_mpris_media_player2_proxy_get_identity;
  iface->get_desktop_entry = nul_org_mpris_media_player2_proxy_get_desktop_entry;
  iface->get_supported_uri_schemes = nul_org_mpris_media_player2_proxy_get_supported_uri_schemes;
  iface->get_supported_mime_types = nul_org_mpris_media_player2_proxy_get_supported_mime_types;
}

/**
 * nul_org_mpris_media_player2_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2.top_of_page">org.mpris.MediaPlayer2</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_org_mpris_media_player2_proxy_new_finish() to get the result of the operation.
 *
 * See nul_org_mpris_media_player2_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
nul_org_mpris_media_player2_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2", NULL);
}

/**
 * nul_org_mpris_media_player2_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_org_mpris_media_player2_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with nul_org_mpris_media_player2_proxy_new().
 *
 * Returns: (transfer full) (type NulOrgMprisMediaPlayer2Proxy): The constructed proxy object or %NULL if @error is set.
 */
NulOrgMprisMediaPlayer2 *
nul_org_mpris_media_player2_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return NUL_ORG_MPRIS_MEDIA_PLAYER2 (ret);
  else
    return NULL;
}

/**
 * nul_org_mpris_media_player2_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2.top_of_page">org.mpris.MediaPlayer2</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See nul_org_mpris_media_player2_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type NulOrgMprisMediaPlayer2Proxy): The constructed proxy object or %NULL if @error is set.
 */
NulOrgMprisMediaPlayer2 *
nul_org_mpris_media_player2_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2", NULL);
  if (ret != NULL)
    return NUL_ORG_MPRIS_MEDIA_PLAYER2 (ret);
  else
    return NULL;
}


/**
 * nul_org_mpris_media_player2_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like nul_org_mpris_media_player2_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call nul_org_mpris_media_player2_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See nul_org_mpris_media_player2_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
nul_org_mpris_media_player2_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2", NULL);
}

/**
 * nul_org_mpris_media_player2_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nul_org_mpris_media_player2_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with nul_org_mpris_media_player2_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type NulOrgMprisMediaPlayer2Proxy): The constructed proxy object or %NULL if @error is set.
 */
NulOrgMprisMediaPlayer2 *
nul_org_mpris_media_player2_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return NUL_ORG_MPRIS_MEDIA_PLAYER2 (ret);
  else
    return NULL;
}

/**
 * nul_org_mpris_media_player2_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like nul_org_mpris_media_player2_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See nul_org_mpris_media_player2_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type NulOrgMprisMediaPlayer2Proxy): The constructed proxy object or %NULL if @error is set.
 */
NulOrgMprisMediaPlayer2 *
nul_org_mpris_media_player2_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2", NULL);
  if (ret != NULL)
    return NUL_ORG_MPRIS_MEDIA_PLAYER2 (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * NulOrgMprisMediaPlayer2Skeleton:
 *
 * The #NulOrgMprisMediaPlayer2Skeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * NulOrgMprisMediaPlayer2SkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #NulOrgMprisMediaPlayer2Skeleton.
 */

struct _NulOrgMprisMediaPlayer2SkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_nul_org_mpris_media_player2_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_nul_org_mpris_media_player2_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_nul_org_mpris_media_player2_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nul_org_mpris_media_player2_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _nul_org_mpris_media_player2_skeleton_vtable =
{
  _nul_org_mpris_media_player2_skeleton_handle_method_call,
  _nul_org_mpris_media_player2_skeleton_handle_get_property,
  _nul_org_mpris_media_player2_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
nul_org_mpris_media_player2_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return nul_org_mpris_media_player2_interface_info ();
}

static GDBusInterfaceVTable *
nul_org_mpris_media_player2_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_nul_org_mpris_media_player2_skeleton_vtable;
}

static GVariant *
nul_org_mpris_media_player2_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_nul_org_mpris_media_player2_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _nul_org_mpris_media_player2_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _nul_org_mpris_media_player2_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _nul_org_mpris_media_player2_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.mpris.MediaPlayer2", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _nul_org_mpris_media_player2_emit_changed (gpointer user_data);

static void
nul_org_mpris_media_player2_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _nul_org_mpris_media_player2_emit_changed (skeleton);
}

static void nul_org_mpris_media_player2_skeleton_iface_init (NulOrgMprisMediaPlayer2Iface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (NulOrgMprisMediaPlayer2Skeleton, nul_org_mpris_media_player2_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (NulOrgMprisMediaPlayer2Skeleton)
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2, nul_org_mpris_media_player2_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (NulOrgMprisMediaPlayer2Skeleton, nul_org_mpris_media_player2_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2, nul_org_mpris_media_player2_skeleton_iface_init));

#endif
static void
nul_org_mpris_media_player2_skeleton_finalize (GObject *object)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (object);
  guint n;
  for (n = 0; n < 9; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (nul_org_mpris_media_player2_skeleton_parent_class)->finalize (object);
}

static void
nul_org_mpris_media_player2_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 9);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_nul_org_mpris_media_player2_emit_changed (gpointer user_data)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.mpris.MediaPlayer2",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_nul_org_mpris_media_player2_schedule_emit_changed (NulOrgMprisMediaPlayer2Skeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
nul_org_mpris_media_player2_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _nul_org_mpris_media_player2_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
nul_org_mpris_media_player2_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 9);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _nul_org_mpris_media_player2_schedule_emit_changed (skeleton, _nul_org_mpris_media_player2_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
nul_org_mpris_media_player2_skeleton_init (NulOrgMprisMediaPlayer2Skeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = nul_org_mpris_media_player2_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_SKELETON, NulOrgMprisMediaPlayer2SkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 9);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[7], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[8], G_TYPE_STRV);
}

static gboolean
nul_org_mpris_media_player2_skeleton_get_can_quit (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean
nul_org_mpris_media_player2_skeleton_get_fullscreen (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean
nul_org_mpris_media_player2_skeleton_get_can_set_fullscreen (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean
nul_org_mpris_media_player2_skeleton_get_can_raise (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean
nul_org_mpris_media_player2_skeleton_get_has_track_list (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
nul_org_mpris_media_player2_skeleton_get_identity (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
nul_org_mpris_media_player2_skeleton_get_desktop_entry (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
nul_org_mpris_media_player2_skeleton_get_supported_uri_schemes (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[7]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
nul_org_mpris_media_player2_skeleton_get_supported_mime_types (NulOrgMprisMediaPlayer2 *object)
{
  NulOrgMprisMediaPlayer2Skeleton *skeleton = NUL_ORG_MPRIS_MEDIA_PLAYER2_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[8]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
nul_org_mpris_media_player2_skeleton_class_init (NulOrgMprisMediaPlayer2SkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = nul_org_mpris_media_player2_skeleton_finalize;
  gobject_class->get_property = nul_org_mpris_media_player2_skeleton_get_property;
  gobject_class->set_property = nul_org_mpris_media_player2_skeleton_set_property;
  gobject_class->notify       = nul_org_mpris_media_player2_skeleton_notify;


  nul_org_mpris_media_player2_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = nul_org_mpris_media_player2_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = nul_org_mpris_media_player2_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = nul_org_mpris_media_player2_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = nul_org_mpris_media_player2_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (NulOrgMprisMediaPlayer2SkeletonPrivate));
#endif
}

static void
nul_org_mpris_media_player2_skeleton_iface_init (NulOrgMprisMediaPlayer2Iface *iface)
{
  iface->get_can_quit = nul_org_mpris_media_player2_skeleton_get_can_quit;
  iface->get_fullscreen = nul_org_mpris_media_player2_skeleton_get_fullscreen;
  iface->get_can_set_fullscreen = nul_org_mpris_media_player2_skeleton_get_can_set_fullscreen;
  iface->get_can_raise = nul_org_mpris_media_player2_skeleton_get_can_raise;
  iface->get_has_track_list = nul_org_mpris_media_player2_skeleton_get_has_track_list;
  iface->get_identity = nul_org_mpris_media_player2_skeleton_get_identity;
  iface->get_desktop_entry = nul_org_mpris_media_player2_skeleton_get_desktop_entry;
  iface->get_supported_uri_schemes = nul_org_mpris_media_player2_skeleton_get_supported_uri_schemes;
  iface->get_supported_mime_types = nul_org_mpris_media_player2_skeleton_get_supported_mime_types;
}

/**
 * nul_org_mpris_media_player2_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2.top_of_page">org.mpris.MediaPlayer2</link>.
 *
 * Returns: (transfer full) (type NulOrgMprisMediaPlayer2Skeleton): The skeleton object.
 */
NulOrgMprisMediaPlayer2 *
nul_org_mpris_media_player2_skeleton_new (void)
{
  return NUL_ORG_MPRIS_MEDIA_PLAYER2 (g_object_new (NUL_TYPE_ORG_MPRIS_MEDIA_PLAYER2_SKELETON, NULL));
}

