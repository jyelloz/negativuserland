language: c
sudo: false
os:
  - linux

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-4.9
      - clang
      - python3
      - bsdtar
      - libstfl-dev

compiler:
  - gcc-4.9

script:
  - prefix="${PWD}/pfx"
  - export PATH="${prefix}/bin:${PATH}"
  - make -f .travis.mk PREFIX="${prefix}"
  - make MESON=meson.py MESONCONF=mesonconf.py NINJA=ninja

notifications:
  email: false
